# V2 迭代路线与证据
唯一需求基线：PRD.md；关键决策：decisions.md；架构：architecture.md。
状态：已确认≠已实现≠已验收。更新时间：2026-09-20。

| 阶段 | 交付 | 完成门槛 | 当前状态 |
|---|---|---|---|
| M0 | 新文档/基线审计、模型/引擎/预算验证 | 运行方案和预算有证据 | 文档/引擎/本机完整E2E完成；RDS/OSS/0.59元账单与平台库账号控制面已验收；V2专用函数/同VPC/ICP备案尚未验收，部署证据门已实现并阻断当前工作流 |
| M1 | 需求理解、React 代码工作台、真实 SQL、调试修正和版本 | 新数据/新指标结果可核对；单独统计代码阶段完成率 | 20/20真实模型+Spark代码阶段通过；七阶段只读旅程已用真实历史委托和1280页面验收；新增统一Data Agent意图路由，真实模型验收待执行，仍不等于Agent自主完整E2E |
| M2a | 调度文件与部署文件、DAG/日历/依赖、版本包 | 文件可解析、可执行，绑定已核验代码版本 | 新增代码Agent受控后台自动生成不可变包与真实Spark文件演练；GUI/API/CLI/MCP同源，人工审阅后才可进入M2b；外部云适配待实现 |
| M2b | 审阅批准、真实部署上线、调度批次与回滚 | 审阅、批准绑定哈希；发布版本被实际调度执行 | 本机四项审阅确认、摘要/演练绑定、审批、版本激活、墙上时钟批次及回滚已验收且多端同源；云端/公网未实现 |
| M2c | 标杆任务上线后监控、告警关联与受控恢复 | 观察真实批次；完成完整链路评测，目标 ≥85% | 当前本机评测20/20、40批次、3阻塞/1取消/4救援；每例包含独立审阅记录并在Spark3.5.9重跑，发布监控已供GUI/API/CLI/MCP读取；公网与更广输出契约仍待后续 |
| M3 | DAPI/XAPI、授权调用、OpenAPI、监控 | 外部示例系统取得真实结果 | 本机已验收：真实DAPI/XAPI、Agent、授权/撤销、版本切换、限流、OpenAPI、调用日志及GUI/API/CLI/MCP；公网未实现 |
| M4 | 接入、治理、质量、报表、实时、运维扩展 | 每模块人工/API/Agent 实测 | M4a—M4f已本机验收；M4g跨模块运维核心已验收；M4h数据契约版本/兼容/SLO/告警闭环已本机验收；真实外部运维模型诊断因隐私门待单独授权 |
| M5 | 公司适配方法、复刻手册、最终 PPT | 可复刻标杆链路 | 复刻手册V1已完成；正式PPT按约定在最终范围完成或用户明确要求时生成 |

## M0 已核对事实
- 旧后端为 Node.js，元数据写入 OSS 单 JSON；不适合直接当作多用户生产库。
- 旧 Agent 使用关键词路由；旧质量/报表/实时指标多数预设；旧发布没有实际调度执行。
- 当前开发机可运行 Node.js 24、Java 17/Python 3.12/Spark 3.5.9，最新35项引擎/交叉/测试SQL检查通过；未安装 Docker。历史3.5.7证据保留但不用于新云部署。
- 模型凭证已保存，真实 Qwen 调用、Spark 报错反馈和自动修正已执行；失败和验证范围升级记录见 [真实试跑报告](m1-live-agent-report.md)。
- 当前云资源实际账单、域名、备案未验证；不能把一次代码试跑推定为模型整体能力达标。
- 公网域名`shuduoai.cn`已购买并写入受保护环境的非秘密目标变量；ICP备案按注册等待期至少3天后才能提交。DNS、证书和公网验收尚未开始，不能将“已购域名”计为“公网已部署”。
- UI 为大标题/长横向导航；V2 改为按工作组织的侧栏与开发工作台。

## 验收记录
实施证据统一写入 [acceptance.md](acceptance.md)。引擎测试、模型代码闭环、调度/部署测试、完整 E2E 分别计数。
用户明确允许分阶段完成；完整 E2E 的七个环节及分母规则以 PRD.md 的 FR-014/015 为准。M1 单独跑通不算完整 E2E。
M2c 提前实现标杆任务必要监控；M4 的平台级运维扩展仍保留，不把大而全监控中心当成 M1 前置条件。
不以旧 28 项测试证明新 SQL、权限或模型能力；M0 未闭环前不扩容或替换公网执行入口。

## 下一批工作顺序

1. 当前冻结20条LOCAL_ACTUAL完整E2E已达到100%；下一轮扩充不同指标、输出契约、权限失败和跨模块任务，不能把4类同一客户资产口径当作全部开发能力。
2. 七阶段证据旅程先让一个真实代码Agent任务的后续人工/调度步骤可见；下一迭代建立持续任务编排与风险分级审批，再单独统计Agent自主完成率，不能用只读关系图提升85%分子。
3. 交付准备已真实自动执行并停在人工审阅；本机审阅已拆为四项确认与独立记录，审批不能绕过。下一步在明确版本/风险批准后建设受控发布与上线后运维Agent，不让“有审批按钮”替代自动化或权限继承。
4. 阿里云OAuth、独立`platform_meta`和最小权限`platform_app`控制面已核验；PR #59/#60/#62/#63—#66均已合并，`v2-staging`非秘密变量和`JASONSECRETS`已存在。最新预置已通过配置、CI、Node24构包、OIDC和账单门，停在部署角色缺少RAM读取；精确vSwitch动作已修正并生成幂等最小策略应用器。下一步在Cloud Shell写入并复核已获批策略，再重跑同一预置工作流；仍需真实MySQL/OSS冷启动、隔离Worker、域名备案和公网验收。
5. M3、M4a、M4b已完成对应资源的GUI/API/CLI/MCP同源；后续经典模块继续按“真实数据面→API→人工流程→Agent→多端验收”顺序建设。
6. 模块布局、可访问性和运行证据持续一起验收；公网门槛通过前，本机发布状态不转换为公网状态。

M3本机验收已完成。下一步进入数据源/元数据与离线同步真实化；先建立可重复的CSV→业务SQLite接入和增量同步基线，再扩展真实MySQL与实时链路，不使用旧V1模拟状态。

M4a本机验收完成：CSV真实全量4行；增量读取2行、更新1、新增1、最终5行；8字段源版本识别新增currency，旧任务失败留存；Agent/GUI/API/CLI/MCP同源。SERVER_MYSQL现支持服务端凭证+表白名单+显式开关下的有界FULL和全表快照差异UPSERT，虚构适配器2行全量后更新1/新增1/最终3行；不称CDC。真实同VPC RDS业务源、独立只读账号与云端数据面仍待验收。

M4b本机验收完成：受控负价格在offset2失败，Checkpoint保留前2事件；修正版本从offset2恢复，最终5个唯一事件、1个重复事件、3只证券状态和3个Checkpoint，告警解除。真实Qwen用508 Token生成方案，人工应用后运行数为0；随后从GUI显式启动并真实追平。V2 CLI/MCP读取同一监控资源，1280/768页面均无横向溢出。

M4c本机验收完成：目录自动投影15个当前逻辑资产并合并重复发布批次；`raw_positions`的2条直接上游绑定包含7字段映射，不再误带兄弟资产。持仓市值在5行上实际汇总为股票10750.00、基金300.00、债券550.00；证券代码标准5/5通过。真实Qwen使用3040 Token引用已登记发布数据集、3张输入表和4个下游服务并披露血缘边界。GUI/API/CLI/MCP同源，1280/768无页面级溢出。

M4h数据契约本机验收完成：`raw_positions_contract`从7字段实际资产证据创建V1并检查5行通过；兼容新增可空`currency`生成V2后实际检查因缺字段失败并打开告警；删除该字段被识别为破坏性变更，未经确认按钮禁用，确认后生成V3，复检100%通过且告警关联恢复。检查真实执行99%行通过率和86400秒新鲜度SLO；GUI/API/CLI/MCP同源，全量181/181。无行或时间证据固定为PARTIAL；公网、多项目契约审批、发布阻断和SQL字段级影响仍待后续。

M4d本机验收完成：持仓市值范围V1在5行中真实失败1行并打开告警；V2将上限从5000.00校准至10000.00后5/5通过，告警关联恢复运行，V1/失败记录均保留。真实Qwen用1046 Token生成security_code非空规则，人工应用后运行数0。GUI通过界面再跑一批5/5；API/CLI/MCP同源，全量126/126，1280/768无溢出。下一步M4e建设用户/项目权限、行列控制、脱敏预览、审批与审计。

M4e本机验收完成：财富顾问策略从5行实际持仓中只返回名下4行，position_id全遮盖、client_id部分脱敏；审计员无策略被拒绝，申请经数据负责人审批后才取得24小时READ_MASKED并返回5行脱敏结果。真实Qwen用1103 Token生成工程师最小权限策略，应用不触发查询。GUI完成策略查询与申请审批，API/CLI/MCP同源，全量131/131，1280/768无溢出。当前身份头不是公网认证；下一步M4f建设数据集、图表、导出和受治理问数。

M4f本机验收完成：持仓数据集实际物化5行快照，4组件报表得到总持仓11600.00、证券4只、资产类别和行业分布；客户资产数据集物化2行已发布结果，总资产2750.00，客户维度只显示CLI***001/002。聚合CSV通过GUI导出且不含明细。真实Qwen用1258 Token生成4组件草稿，应用后运行数0。GUI/API/CLI/MCP同源，全量137/137，1280/768无溢出。下一步M4g聚合各模块运行、告警、成本和审计为平台运维中心。

M4g核心本机验收：42条实际运行活动归一为5个事故，1个离线同步事故开放、4个实时/质量/发布事故关联真实恢复；事故解除器验证同资源和时间顺序。GUI/API/CLI/MCP同源，全量143/143，1280/768无溢出。真实Qwen诊断两次在发送前被安全策略拒绝；随后增加别名化适配器和默认412隐私门，页面禁用按钮并明确待授权。因此M4g的人工/API/多端/隐私边界通过，真实模型诊断仍是明确待办，不能记为已验收。

当前完整E2E正式评测`f3e1e908-3d27-4622-a2cb-90b246f827bb`完成：20条全部成功，率100%，40个实际调度Spark批次；每条先在Spark3.5.9重跑冻结的真实模型SQL，新增独立审阅记录作为必经阶段。保留非交易日、错误摘要、幂等冲突3次BLOCKED，1次CANCELLED，均经4次救援完成。运维页、API、CLI、MCP展示同一报告并明确LOCAL_ACTUAL/公网未部署。评测器审阅不是生产受邀用户批准，详见[完整链路报告](full-lifecycle-evaluation.md)。

公网认证本机预览完成：匿名浏览写按钮禁用且后端401；管理员登录后创建一次性邀请，邀请兑换为ENGINEER后开发写入启用；工程师看不到邀请管理且公网模型Key输入完全移除。Cookie/CSRF/权限攻击面测试与768响应式通过。下一步必须把同一认证数据写入云端持久库并在HTTPS公网验证，当前仍不是已部署公网会话。

云端持久化实现完成且本机冷启动验收：MySQL revision CAS保存平台元数据；OSS ETag CAS保存离线落地行、实时状态/Checkpoint、DAPI业务快照和报表快照；后台写入触发协调保存，请求成功前等待持久化。模拟函数重启后，控制面记录与实际落地行同时恢复；陈旧写入返回409。全量CI 159/159。真实RDS/OSS连接、FC单实例配置与HTTPS公网仍待账号内验证，详见[云端持久化报告](cloud-persistence.md)。

V2云包与部署门已准备：独立最小依赖锁、Linux x64 Node24自带运行时包、70MiB上限、专用函数手动工作流、部署后MySQL/OSS/认证/匿名拒写检查。当前Mac只验证了脚本语法与非Linux失败关闭；必须在GitHub Linux CI构建并在阿里云只读核验后才能执行部署。详见[云部署说明](v2-cloud-deployment.md)。

公网隔离Spark的控制面/Worker协议已实现并本机验收：启动健康探测、HTTPS限制、项目/时间戳/Nonce/正文HMAC、防重放、白名单表、2MiB默认上下行限制、120秒超时、取消和独立断言复核；加入3.5.9云交付绑定后全量CI 166/166。W1已打包并从包内真实运行，W2计划显式挂载三种官方运行时层并使用私有OSS代码位置；尚未创建Worker函数或验证FC私网/函数鉴权和费用，因此公网执行仍保持关闭。

Worker构建门已在Linux双构建和包内Spark冒烟通过：云端从历史Spark3.5.7升级为3.5.9，PySpark/py4j/sqlglot哈希固定；大包经私有OSS部署，GitHub构建产物只保留1天。W2失败关闭计划已固定官方层、精确对象、实例边界与未授权W3 Invoke权限；真实OSS上传、FC创建和Cloud Shell烟测仍待执行。分级门槛见[隔离Spark Worker](isolated-spark-worker.md)。

W2新增OSS对象证据门：上传后核对实际字节、摘要元数据和ETag，脱敏输出只保留对象键哈希；Bucket私有性和禁止覆盖历史仍须独立证据。该门未运行真实OSS，本轮不把验证器测试计作W2云健康。

W2新增OSS私有访问四证门：Bucket/Object ACL、Bucket Policy公开状态和Bucket Public Access Block必须同时安全；缺失证据和任一公共状态均阻断。验证器仅本机测试，待Cloud Shell主账号真实读取后才可把`publicAccessVerified`置为true。

W2新增FC创建后联合验收器：GetFunction/并发/弹性必须同时满足运行时层、路径、资源、VPC、秘密、无公网出站、1并发/1预留/最小0等约束；响应脱敏且固定非公网/未连控制面。当前只有合成响应测试，待真实创建后执行。

W2新增FC私有Invoke适配：默认关闭，健康事件脱敏；签名执行事件复用现有HMAC/Nonce/项目/白名单/断言入口，重放仍被拒绝。当前只在本机回环验证，待真实FC由Cloud Shell主账号调用；控制面自动Invoke仍属于W3。

W2新增固定证券烟测：Cloud Shell只发送无参数操作名，Worker内部构造固定虚构数据并自签名，返回脱敏摘要；不向人工暴露共享密钥或开放任意SQL。当前包已在同一main提交双构建：318,914,420字节、SHA-256 `f5083be5…4118`一致，5套回归和tests.sql通过；W1重新验收，仍待W2真实云。

W2精确对象权限已实现独立渲染/幂等应用器：只含当前摘要对象Get/Put，同名策略漂移失败关闭，无List/Delete/Invoke。2026-09-20已获用户明确授权；当前因Cloud Shell会话过期尚未执行apply，不能计为策略已附加或W2已部署。

W2不可变上传已新增独立手动工作流：受保护环境OIDC、当前main重构建、SHA/字节双绑定、锁定ossutil、明确NotFound后禁止覆盖Put及Head证据；上传与FC创建分离。该工作流尚未运行，且即使上传成功仍须Cloud Shell四项OSS私有性门，不能升级W2状态。

W2创建第二门已代码化：Cloud Shell生成六小时脱敏OSS收据，绑定当前包、对象键哈希和上传运行；手动工作流先完整分页核算当月CNY账单，再核验网络和函数不存在，使用0600秘密文件创建固定私有Worker并回读规格/并发/缩零。当前仅代码和合成证据，尚未生成真实收据或创建函数；私有固定烟测仍是独立最后门。

W2两个已批准RAM策略新增联合幂等应用器：一次校验、应用、附加和回读，正文漂移失败关闭，禁止策略版本替换/解绑/删除。当前仍未在真实Cloud Shell执行；该工具降低人工操作但不改变授权范围。

控制面预置、控制面更新和Worker创建三条工作流的实时账单门已统一为QueryBill完整分页脚本，与已批准`bss:DescribeBillList`一致；金额保守合计、错误脱敏且达到200元停止。真实OIDC运行仍待策略应用后验证。

受保护配置新增独立非云校验工作流：实际展开`JASONSECRETS`并检查平台库、管理员哈希、模型、Worker/调度密钥及可选业务源完整性；不申请OIDC、不调用阿里云、不回显值。当前仅代码与合成测试，须在GitHub `v2-staging`真实运行后才能关闭“只确认Secret名称”的缺口。

首次真实运行`35489413863`已证明现有bundle含平台库、管理员哈希和模型Key占位/无效值，配置门正确失败；并发现GitHub作业env摘要会显示普通云变量。后续校验改用合成非秘密配置，避免在公开日志重复资源标识；用户修正受保护内容前控制面和Worker均不部署。

逐项Secret覆盖语义已修正：展开器先排除已有逐项值，再校验剩余bundle，允许按键逐步替换旧占位配置而不被其阻断。下一步由Codex安全写入本机已有百炼Key及生成的Worker/调度密钥；平台MySQL密码和管理员登录口径仍需用户掌握的真实值或Cloud Shell重置方案。

独立Data Agent的数据开发工具升级为V2双语言契约：工作台直接选择Spark SQL/受限Python；Python通过真实模型适配、不可变版本、受限子进程和五套断言形成可恢复专业任务，继续继承批准/幂等/取消/任务图。通用文件交付已在下一增量补齐；云Python Worker与正式发布仍明确阻断。

Python交付首版已补齐通用文件和本机发布阶段：独立`shuduo-python-delivery/v1`生成main.py、T+1调度、部署清单、冻结输入和验证报告，按文件重新执行受限CPython与五套断言；工程师审阅/审批后，统一调度器真实触发两个Python批次并进入监控/告警/回滚。云Python Worker、外部持久调度、公网验收和Python结果DAPI仍未完成。

经典数据开发新增受限Python首版：真实CPython子进程、版本/运行/取消、五套证券断言及GUI/API/CLI/MCP同源已实现；禁止import/文件/网络/反射并回传CPU/文件/内存限制证据。当前仅`LOCAL_RESTRICTED_PYTHON`，macOS地址空间上限未验证，云隔离、Python Agent生成、第三方包和调度交付仍待后续。

本机项目隔离运行时已实际升级到Spark3.5.9/py4j0.10.9.9；35/35真实Spark用例在新版本重新执行通过，耗时23.891秒。该结果证明本机兼容升级，不证明Linux云Worker包或FC隔离执行。

版本化交付产物已接入统一存储：本机权限0600不可变文件与云端OSS create-only对象共用接口；创建包先保存对象，审批、演练和发布前重新校验正文、大小、摘要和对象键，旧包按需迁移。3项新增产物测试使全量CI达到169/169；真实OSS写入/读取与生命周期仍待账号验收。

公网管理员引导已去除长期明文密码：本机stdin工具生成scrypt哈希，GitHub/FC只接收哈希，cloud-server检测到旧明文变量即拒绝启动；登录验证与非法哈希测试通过。正式改密/轮换仍是公网认证后续项。

登录后自主改密已补齐：设置页验证当前密码、两次新密码和强度；后端更新scrypt哈希、撤销旧会话并签发新会话，审计不含秘密。全量CI 170/170；忘记密码恢复与邮件通道仍待后续。

应用内月度预算门已完成：模型Token按官方档位估算，缺usage和未知模型保守计费；远程Spark按运行数/实际秒数并预留下一次额度。默认模型50元、Spark200次/3600秒，已核验账号支出达到200元统一硬停；超限在任务创建前429。预算状态进入GUI/API/CLI/MCP，全量CI 174/174。账号账单自动连接仍待真实云核验。

阿里云只读预检已收敛为单脚本：一次查询RDS/FC/OSS/BSS并输出0600脱敏决策记录，不写账号ID、资源名/ID、地址、角色或环境值。伪CLI测试确认敏感诱饵全部被清除。真实执行仍等待Cloud Shell登录，详见[阿里云只读预检](aliyun-readonly-audit.md)。

M5复刻手册V1已形成：明确不是照搬本机代码，而是把统一原子API、Agent任务闭环、风险分级审批、真实执行/断言和评测方法接入公司现有中台。包含产品经理与Codex/研发双路线、8个里程碑、90天上限建议和证券标杆旅程。正式PPT继续遵守“最终范围完成或用户明确要求时再生成”。详见[公司复刻手册](company-replication-playbook.md)。

云端持久调度核心已本机验收：到期ACTIVE_CLOUD批次由外部HMAC tick领取，先持久化租约再执行，过期租约可恢复，ACTIVE_LOCAL不被云tick改变；签名重放被拒绝，默认关闭的GitHub cron适配器不依赖FC进程存活。当前发布API尚未生成真实ACTIVE_CLOUD版本，FC冻结/真实Spark/告警恢复也未云端验收，因此不能把该核心标为调度上线。详见[云端持久调度](durable-cloud-scheduler.md)。

Agent-first独立工作台第一版已实现：默认首页从数据开发切换为Data Agent，用户可在同一会话查看任务历史、上下文、跨域计划、风险、专业Agent活动、草稿应用和证据轨迹；经典模块成为可选接管入口。父意图已持久关联专业任务并可在历史会话恢复；REQUEST_APPROVAL批准由专业任务消费，PLAN_ONLY不能关联执行；统一任务图从现有证据投影各步骤完成度并供四端读取。当前仍需补齐统一工具Schema/补偿协议、自动推进低风险步骤并单独运行公网Agent-first E2E。详见[Agent-first产品蓝图](agent-first-product-blueprint.md)。

工具目录已补齐严格输入/输出Schema、稳定版本和完整契约摘要；GUI在生成步骤批准或专业任务前调用服务端预检，CLI/MCP可执行同源预检。该能力只防止旧客户端和错误参数误调用，固定`NO_EXECUTION`；下一步仍是统一调用/补偿协议、低风险自动推进和公网Agent-first E2E，不能把契约预检计作专业任务执行。

统一专业工具调用协议已复用现有十域原子API：父意图、目录摘要、步骤输入和批准同时校验，创建成功后在服务端绑定handoff及任务图，GUI/CLI/MCP不再各自拼装专业调用和关联。持久调用租约以步骤和输入摘要去重；超时/中断可从已创建子任务继续绑定，处理中并发与输入漂移失败关闭。该协议解决调用一致性和首个半成功恢复边界，不改变草稿应用、发布、授权和高成本操作的独立关口；后续继续扩展跨步骤补偿、低风险自动推进与公网E2E。

工具调用租约已纳入冷启动恢复：CLAIMED/TASK_CREATED在重启后显式INTERRUPTED，任务图展示恢复状态、尝试号和待绑定子任务；用户重新批准会保留旧证据并创建新专业尝试。未绑定成功不计完成且不能应用草稿，运行中孤儿任务可由统一子任务取消。下一步仍需跨步骤补偿/回滚、低风险自动推进和真实云冷启动验证。

## 本轮M2a准备进展

在不把M1更广泛评测标为完成的前提下，复用已核验标杆版本完成文件交付与本机演练。
UI/API和CLI均已真实读取交付目录并运行Spark与测试SQL，见 [M2a规范和证据](m2a-delivery.md)。
本段记录M2a完成当时的边界；随后进展以下一节为准。本机能力即使完成，也不得改名为公网或生产发布成功。

## 2026-09-14：M1扩展评测与M2b/M2c本机验收

- M1固定20例由4类开发意图与5套数据上下文交叉组成。首轮因评测器幂等键超过API上限仅4例真正提交，4/20不能归因于模型；缺陷修复后同一冻结集合20/20完成，33次模型尝试、37,559 Token，仍只计SQL开发阶段。
- M2b把成功文件演练、人工审批、包摘要、发布版本和实际批次绑定。首个版本由墙上时钟触发2个真实Spark批次，均通过测试SQL及5套回归。
- M2c受控破坏发布目录的main.sql摘要：一个批次先成功，随后两个计时批次真实拒绝并各产生开放告警；文件按可信交付副本恢复后，回滚到最近健康版本，两个告警由回滚事件解除，并由计时器再触发2个Spark恢复批次成功。
- 1280与768宽度均无页面级横向溢出；768下审批与监控改为单列，控制台无错误。证据和边界见[M1报告](m1-live-agent-report.md)与[M2b/M2c报告](m2b-m2c-local-release.md)。
