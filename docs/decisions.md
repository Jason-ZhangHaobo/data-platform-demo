# 已确认决策与实施假设
基线日期：2026-09-13。状态分为：已确认、待验证、已实现、已验收。变更必须记录替代关系。

| 决策 | 状态 | 内容及原因 |
|---|---|---|
| D-01 | 已确认 | 验证成熟中台如何智能化，服务产品经理工作与公司借鉴 |
| D-02 | 已确认 | 经典功能全面 + Data Agent 深度融合，不缩为单聊天产品 |
| D-03 | 已确认 | 工程师优先；代码→调度部署→找数据 |
| D-04 | 已确认 | 首期给定表结构/口径，Agent 委托式推进，人审关键结果 |
| D-05 | 已确认 | SQL、测试、调度、部署文件必须通用可运行且可导出版本化 |
| D-06 | 已确认 | DAPI=参数化查询；XAPI=组合编排，后续受控代码 |
| D-07 | 已确认 | 独立公网验证站；公开体验、受邀后创建任务 |
| D-08 | 已确认 | 内地部署，用户配合域名实名和备案 |
| D-09 | 已确认 | 200元/月上限；少量用户、小数据、计算串行 |
| D-10 | 已确认 | 统一 GUI/API/CLI/MCP，复用成熟引擎 |
| D-11 | 已确认 | React/TypeScript 代码工作台，真实 Spark；模型可替换 |
| D-12 | 已确认 | UI 现代、统一、有审美、易理解；实际交互和响应式必须验收 |
| D-13 | 已确认 | 需求、边界、决策、验收、路线写入项目文件，取代旧方案 |
| D-14 | 已确认 | 不发布任何公司截图、地址、业务数据或凭证 |
| D-15 | 待验证 | Qwen Coder 用真实用例评估，不凭名称声称最佳 |
| D-16 | 待验证 | 云资源账单及 RDS 到期成本需账号内只读核验 |
| D-17 | 实施假设 | 未知关键业务口径暂停追问，已有规范自动复用 |
| D-18 | 实施默认 | 开发环境可用独立 SQLite 平台库；云端使用独立 MySQL 元数据库，不把 SQLite 当 Spark |
| D-19 | 实施默认 | 本地 Spark 进程用于引擎/结果验证；生产须在隔离执行单元与身份边界验收后开放 |
| D-20 | 已实现 | 平台设置提供本地密钥输入，不要求非技术用户复制命令；仅写权限 0600 的本项目配置，云端仍需独立秘密管理 |
| D-21 | 已确认，2026-09-13 | ≥85% 指完整链路：需求理解、代码、调试、调度文件、部署文件、真实上线、上线后监控；SQL 跑通不得替代 |
| D-22 | 已确认，2026-09-13 | 用户允许分阶段完成；M1/M2a/M2b/M2c 分别验收，保留最终完整目标，不一次性交付全部 |
| D-23 | 实施默认，待 M2 固化 | 标杆最小监控观察至少两个实际调度批次；故障用例验证告警、诊断和恢复；测试周期与生产周期明确区分 |
| D-24 | 实施默认，2026-09-14 | 保留M1更广泛评测待办，基于已验证标杆SQL先落地M2a文件生成/本机演练子能力；不新增收费云资源，不越过M0云端门槛 |
| D-25 | 已实现，2026-09-14 | 文件采用版本化JSON规范和SHA-256；本期只支持local-spark-v1固定三节点，不以生成配置冒充DataWorks/EMR适配 |
| D-26 | 已实现，2026-09-14 | 测试SQL由真实Spark执行，演练声明非定时器触发、未发布；样例日历不能替代证券交易所官方日历 |
| D-27 | 已验收，2026-09-14 | M1冻结20例代码阶段评测：4类开发意图×5套合成数据；失败/未执行保留，阶段通过率与完整E2E严格分开 |
| D-28 | 已验收，2026-09-14 | M2b本机发布要求成功演练、人工审批、包摘要与代码哈希绑定；短周期墙上时钟触发至少两个实际Spark批次，刷新可恢复记录 |
| D-29 | 已验收，2026-09-14 | M2c本机监控保存批次事件、健康度和告警；受控完整性故障必须真实失败，回滚只恢复已有成功批次的历史版本并再跑两个计时批次 |
| D-30 | 已确认，2026-09-14 | 本机发布、短周期调度与监控不是公网/生产验收；所有接口和页面同时保留publicDeployed=false与fullLifecycleE2E=false |
| D-31 | 本机已验收，2026-09-14 | DAPI只从真实调度成功的发布批次物化数据；业务查询使用独立SQLite，不把平台元数据库当业务服务库，云端仍需独立数据库 |
| D-32 | 本机已验收，2026-09-14 | XAPI首版为声明式组合，固定子DAPI版本并按client_id合并；不开放任意代码节点 |
| D-33 | 本机已验收，2026-09-14 | 本机应用令牌只显示一次，平台仅存SHA-256哈希；CLI/MCP调用从环境变量取令牌，令牌不进入命令参数或模型参数 |
| D-34 | 本机已验收，2026-09-14 | GUI、API、CLI、MCP通过同一/api/v2资源；旧dataplatform CLI/MCP继续明确为V1模拟，不作为V2证据 |
| D-35 | 本机已验收，2026-09-14 | 数据服务Agent只能引用已登记发布批次/DAPI版本，方案经后端校验后人工应用为草稿；模型无权测试、发布或创建令牌 |
| D-36 | 本机已验收，2026-09-14 | 数据源首版仅允许仓库合成目录内普通CSV文件；拒绝目录、符号链接、凭证和5MB以上文件，不读取用户或公司路径 |
| D-37 | 本机已验收，2026-09-14 | 连接测试、元数据和同步分别绑定不可变源版本；测试后内容变化、结构变化或旧任务版本均失败关闭并保留运行证据 |
| D-38 | 本机已验收，2026-09-14 | FULL与INCREMENTAL_UPSERT写入独立落地SQLite；业务行与平台文档分离，重复主键整批回滚，目标摘要只覆盖业务内容 |
| D-39 | 本机已验收，2026-09-14 | 同步请求先创建RUNNING记录再写落地区；幂等键重放终态结果，不确定中断不自动重写，避免用HTTP成功替代同步证据 |
| D-40 | 本机已验收，2026-09-14 | 同步Agent只读取字段元数据和版本摘要，后端验证全部引用，人工应用后只创建READY任务；GUI/API/CLI/MCP共用同一接入资源 |
| D-41 | 本机已验收，2026-09-14 | 实时首版采用local-event-log-v1真实处理有限事件，不以Kafka/Flink模拟指标冒充连接；生产适配器后续独立验收 |
| D-42 | 本机已验收，2026-09-14 | 事件ID提供幂等、sequence控制最新状态，Checkpoint保存offset/前缀/状态摘要；恢复版本在Checkpoint之前有变化时失败关闭 |
| D-43 | 本机已验收，2026-09-14 | 失败运行保留已处理事件和Checkpoint并产生告警；恢复从下一offset继续，成功后告警关联恢复运行，重复事件不重复计入 |
| D-44 | 本机已验收，2026-09-14 | 实时Agent只读取源摘要和固定事件契约；模型方案由后端重新绑定源版本并校验，人工应用只创建READY任务，不自动启动或声称连接Kafka/Flink |
| D-45 | 本机已验收，2026-09-14 | 实时GUI、API、V2 CLI与V2 MCP共用streams资源；工作台同时展示源边界、状态、Watermark、Checkpoint、运行和告警，1280/768均须无页面级横向溢出 |
| D-46 | 本机已验收，2026-09-14 | 资产目录从当前实际资源与证据自动生成，逻辑资产合并多次成功发布批次，避免把每个批次重复展示为一个业务资产 |
| D-47 | 本机已验收，2026-09-14 | 血缘来自同步字段映射、实时任务、Spark发布和DAPI/XAPI版本绑定；上游与下游分别遍历，Spark字段级表达式解析未完成时必须披露 |
| D-48 | 本机已验收，2026-09-14 | 指标和标准首版只绑定有实际行数据的落地/实时状态资产；金额按整数分精确计算，标准失败值只保存哈希，不能用静态结果冒充执行 |
| D-49 | 本机已验收，2026-09-14 | 资产Agent只获得目录摘要和版本绑定边，必须引用已登记资产ID；未知引用失败关闭，不读取业务行、不修改资产，回答同时保留边界说明 |
| D-50 | 本机已验收，2026-09-14 | 质量规则、版本、运行和告警分离保存；检测绑定规则配置摘要与资产证据摘要，失败行只留哈希，不回显业务行 |
| D-51 | 本机已验收，2026-09-14 | 规则修复必须创建新版本，旧版本退役但不删除；新版本复检通过后以恢复运行ID解除原告警，HTTP成功不能替代质量通过 |
| D-52 | 本机已验收，2026-09-14 | 质量Agent只读取字段元数据、规则摘要和聚合失败计数；人工应用只创建未运行规则，模型无权执行、解除告警或访问无效样本 |
| D-53 | 本机已验收，2026-09-14 | 质量GUI/API/CLI/MCP共用同一资源；工作台必须同时展示规则表达、版本、运行、告警和恢复，1280/768无页面级横向溢出 |
| D-54 | 本机已验收，2026-09-15 | 安全首版使用明确标记的四个虚构身份和X-Actor-Id测试策略语义；该头不是身份认证，不能作为公网或跨用户安全证明 |
| D-55 | 本机已验收，2026-09-15 | 安全策略版本化保存角色、行范围、默认动作和字段动作；无策略或未声明字段默认拒绝，财富顾问范围由虚构客户归属实际裁剪 |
| D-56 | 本机已验收，2026-09-15 | 安全查询先裁剪行再处理列，支持部分脱敏、全遮盖、哈希和不可见；审计只存决策、计数、摘要和策略/授权引用，不保存业务行 |
| D-57 | 本机已验收，2026-09-15 | 临时访问先申请后由DATA_OWNER审批，授权带到期时间；普通角色越权审批失败，拒绝和批准均留审计 |
| D-58 | 本机已验收，2026-09-15 | 安全Agent只读取合成身份与字段/策略摘要，人工应用只建策略不查询/审批；GUI/API/CLI/MCP同源并通过1280/768验收 |
| D-59 | 本机已验收，2026-09-15 | 报表数据集草稿不复制数据，显式刷新才把实际资产行写入独立快照库；平台元数据只保留行数、字段与摘要 |
| D-60 | 本机已验收，2026-09-15 | 报表版本固定数据集快照，KPI/柱状/饼图只执行受限聚合；金额按整数分计算，导出只含聚合结果 |
| D-61 | 本机已验收，2026-09-15 | client_id和position_id作为报表维度时在聚合结果生成前默认脱敏；历史未脱敏运行对外读取时同样转换显示，不改写底层证据 |
| D-62 | 本机已验收，2026-09-15 | 报表Agent只读取字段与快照摘要，人工应用后运行数0；GUI/API/CLI/MCP同源，1280/768无页面级横向溢出 |

## 替代关系
旧菜单、关键词路由、固定评分、模拟发布和静态权限标签不构成 V2 验收证据。
旧版保留作对照，访问审批未完成改动留在原分支，不混入 V2。
