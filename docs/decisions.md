# 已确认决策与实施假设
基线日期：2026-09-13。状态分为：已确认、待验证、已实现、已验收。变更必须记录替代关系。

| 决策 | 状态 | 内容及原因 |
|---|---|---|
| D-01 | 已确认 | 验证成熟中台如何智能化，服务产品经理工作与公司借鉴 |
| D-02 | 已确认 | 经典功能全面 + Data Agent 深度融合，不缩为单聊天产品 |
| D-03 | 已确认 | 工程师优先；代码→调度部署→找数据 |
| D-04 | 已确认 | 首期给定表结构/口径，Agent 委托式推进，人审关键结果 |
| D-05 | 已确认 | SQL、测试、调度、部署文件必须通用可运行且可导出版本化 |
| D-06 | 已确认 | DAPI=参数化查询；XAPI=组合编排，后续受控代码 |
| D-07 | 已确认 | 独立公网验证站；公开体验、受邀后创建任务 |
| D-08 | 已确认 | 内地部署，用户配合域名实名和备案 |
| D-09 | 已确认 | 200元/月上限；少量用户、小数据、计算串行 |
| D-10 | 已确认 | 统一 GUI/API/CLI/MCP，复用成熟引擎 |
| D-11 | 已确认 | React/TypeScript 代码工作台，真实 Spark；模型可替换 |
| D-12 | 已确认 | UI 现代、统一、有审美、易理解；实际交互和响应式必须验收 |
| D-13 | 已确认 | 需求、边界、决策、验收、路线写入项目文件，取代旧方案 |
| D-14 | 已确认 | 不发布任何公司截图、地址、业务数据或凭证 |
| D-15 | 待验证 | Qwen Coder 用真实用例评估，不凭名称声称最佳 |
| D-16 | 待验证 | 云资源账单及 RDS 到期成本需账号内只读核验 |
| D-17 | 实施假设 | 未知关键业务口径暂停追问，已有规范自动复用 |
| D-18 | 实施默认 | 开发环境可用独立 SQLite 平台库；云端使用独立 MySQL 元数据库，不把 SQLite 当 Spark |
| D-19 | 实施默认 | 本地 Spark 进程用于引擎/结果验证；生产须在隔离执行单元与身份边界验收后开放 |
| D-20 | 已实现 | 平台设置提供本地密钥输入，不要求非技术用户复制命令；仅写权限 0600 的本项目配置，云端仍需独立秘密管理 |
| D-21 | 已确认，2026-09-13 | ≥85% 指完整链路：需求理解、代码、调试、调度文件、部署文件、真实上线、上线后监控；SQL 跑通不得替代 |
| D-22 | 已确认，2026-09-13 | 用户允许分阶段完成；M1/M2a/M2b/M2c 分别验收，保留最终完整目标，不一次性交付全部 |
| D-23 | 实施默认，待 M2 固化 | 标杆最小监控观察至少两个实际调度批次；故障用例验证告警、诊断和恢复；测试周期与生产周期明确区分 |
| D-24 | 实施默认，2026-09-14 | 保留M1更广泛评测待办，基于已验证标杆SQL先落地M2a文件生成/本机演练子能力；不新增收费云资源，不越过M0云端门槛 |
| D-25 | 已实现，2026-09-14 | 文件采用版本化JSON规范和SHA-256；本期只支持local-spark-v1固定三节点，不以生成配置冒充DataWorks/EMR适配 |
| D-26 | 已实现，2026-09-14 | 测试SQL由真实Spark执行，演练声明非定时器触发、未发布；样例日历不能替代证券交易所官方日历 |
| D-27 | 已验收，2026-09-14 | M1冻结20例代码阶段评测：4类开发意图×5套合成数据；失败/未执行保留，阶段通过率与完整E2E严格分开 |
| D-28 | 已验收，2026-09-14 | M2b本机发布要求成功演练、人工审批、包摘要与代码哈希绑定；短周期墙上时钟触发至少两个实际Spark批次，刷新可恢复记录 |
| D-29 | 已验收，2026-09-14 | M2c本机监控保存批次事件、健康度和告警；受控完整性故障必须真实失败，回滚只恢复已有成功批次的历史版本并再跑两个计时批次 |
| D-30 | 已确认，2026-09-14 | 本机发布、短周期调度与监控不是公网/生产验收；所有接口和页面同时保留publicDeployed=false与fullLifecycleE2E=false |
| D-31 | 本机已验收，2026-09-14 | DAPI只从真实调度成功的发布批次物化数据；业务查询使用独立SQLite，不把平台元数据库当业务服务库，云端仍需独立数据库 |
| D-32 | 本机已验收，2026-09-14 | XAPI首版为声明式组合，固定子DAPI版本并按client_id合并；不开放任意代码节点 |
| D-33 | 本机已验收，2026-09-14 | 本机应用令牌只显示一次，平台仅存SHA-256哈希；CLI/MCP调用从环境变量取令牌，令牌不进入命令参数或模型参数 |
| D-34 | 本机已验收，2026-09-14 | GUI、API、CLI、MCP通过同一/api/v2资源；旧dataplatform CLI/MCP继续明确为V1模拟，不作为V2证据 |
| D-35 | 本机已验收，2026-09-14 | 数据服务Agent只能引用已登记发布批次/DAPI版本，方案经后端校验后人工应用为草稿；模型无权测试、发布或创建令牌 |
| D-36 | 本机已验收，2026-09-14 | 数据源首版仅允许仓库合成目录内普通CSV文件；拒绝目录、符号链接、凭证和5MB以上文件，不读取用户或公司路径 |
| D-37 | 本机已验收，2026-09-14 | 连接测试、元数据和同步分别绑定不可变源版本；测试后内容变化、结构变化或旧任务版本均失败关闭并保留运行证据 |
| D-38 | 本机已验收，2026-09-14 | FULL与INCREMENTAL_UPSERT写入独立落地SQLite；业务行与平台文档分离，重复主键整批回滚，目标摘要只覆盖业务内容 |
| D-39 | 本机已验收，2026-09-14 | 同步请求先创建RUNNING记录再写落地区；幂等键重放终态结果，不确定中断不自动重写，避免用HTTP成功替代同步证据 |
| D-40 | 本机已验收，2026-09-14 | 同步Agent只读取字段元数据和版本摘要，后端验证全部引用，人工应用后只创建READY任务；GUI/API/CLI/MCP共用同一接入资源 |
| D-41 | 本机已验收，2026-09-14 | 实时首版采用local-event-log-v1真实处理有限事件，不以Kafka/Flink模拟指标冒充连接；生产适配器后续独立验收 |
| D-42 | 本机已验收，2026-09-14 | 事件ID提供幂等、sequence控制最新状态，Checkpoint保存offset/前缀/状态摘要；恢复版本在Checkpoint之前有变化时失败关闭 |
| D-43 | 本机已验收，2026-09-14 | 失败运行保留已处理事件和Checkpoint并产生告警；恢复从下一offset继续，成功后告警关联恢复运行，重复事件不重复计入 |
| D-44 | 本机已验收，2026-09-14 | 实时Agent只读取源摘要和固定事件契约；模型方案由后端重新绑定源版本并校验，人工应用只创建READY任务，不自动启动或声称连接Kafka/Flink |
| D-45 | 本机已验收，2026-09-14 | 实时GUI、API、V2 CLI与V2 MCP共用streams资源；工作台同时展示源边界、状态、Watermark、Checkpoint、运行和告警，1280/768均须无页面级横向溢出 |
| D-46 | 本机已验收，2026-09-14 | 资产目录从当前实际资源与证据自动生成，逻辑资产合并多次成功发布批次，避免把每个批次重复展示为一个业务资产 |
| D-47 | 本机已验收，2026-09-14 | 血缘来自同步字段映射、实时任务、Spark发布和DAPI/XAPI版本绑定；上游与下游分别遍历，Spark字段级表达式解析未完成时必须披露 |
| D-48 | 本机已验收，2026-09-14 | 指标和标准首版只绑定有实际行数据的落地/实时状态资产；金额按整数分精确计算，标准失败值只保存哈希，不能用静态结果冒充执行 |
| D-49 | 本机已验收，2026-09-14 | 资产Agent只获得目录摘要和版本绑定边，必须引用已登记资产ID；未知引用失败关闭，不读取业务行、不修改资产，回答同时保留边界说明 |
| D-50 | 本机已验收，2026-09-14 | 质量规则、版本、运行和告警分离保存；检测绑定规则配置摘要与资产证据摘要，失败行只留哈希，不回显业务行 |
| D-51 | 本机已验收，2026-09-14 | 规则修复必须创建新版本，旧版本退役但不删除；新版本复检通过后以恢复运行ID解除原告警，HTTP成功不能替代质量通过 |
| D-52 | 本机已验收，2026-09-14 | 质量Agent只读取字段元数据、规则摘要和聚合失败计数；人工应用只创建未运行规则，模型无权执行、解除告警或访问无效样本 |
| D-53 | 本机已验收，2026-09-14 | 质量GUI/API/CLI/MCP共用同一资源；工作台必须同时展示规则表达、版本、运行、告警和恢复，1280/768无页面级横向溢出 |
| D-54 | 本机已验收，2026-09-15 | 安全首版使用明确标记的四个虚构身份和X-Actor-Id测试策略语义；该头不是身份认证，不能作为公网或跨用户安全证明 |
| D-55 | 本机已验收，2026-09-15 | 安全策略版本化保存角色、行范围、默认动作和字段动作；无策略或未声明字段默认拒绝，财富顾问范围由虚构客户归属实际裁剪 |
| D-56 | 本机已验收，2026-09-15 | 安全查询先裁剪行再处理列，支持部分脱敏、全遮盖、哈希和不可见；审计只存决策、计数、摘要和策略/授权引用，不保存业务行 |
| D-57 | 本机已验收，2026-09-15 | 临时访问先申请后由DATA_OWNER审批，授权带到期时间；普通角色越权审批失败，拒绝和批准均留审计 |
| D-58 | 本机已验收，2026-09-15 | 安全Agent只读取合成身份与字段/策略摘要，人工应用只建策略不查询/审批；GUI/API/CLI/MCP同源并通过1280/768验收 |
| D-59 | 本机已验收，2026-09-15 | 报表数据集草稿不复制数据，显式刷新才把实际资产行写入独立快照库；平台元数据只保留行数、字段与摘要 |
| D-60 | 本机已验收，2026-09-15 | 报表版本固定数据集快照，KPI/柱状/饼图只执行受限聚合；金额按整数分计算，导出只含聚合结果 |
| D-61 | 本机已验收，2026-09-15 | client_id和position_id作为报表维度时在聚合结果生成前默认脱敏；历史未脱敏运行对外读取时同样转换显示，不改写底层证据 |
| D-62 | 本机已验收，2026-09-15 | 报表Agent只读取字段与快照摘要，人工应用后运行数0；GUI/API/CLI/MCP同源，1280/768无页面级横向溢出 |
| D-63 | 本机已验收，2026-09-15 | 运维事故以实际失败运行ID幂等归一，自动关联实时Checkpoint、质量规则版本和发布回滚恢复；事故详情不返回业务行 |
| D-64 | 本机已验收，2026-09-15 | 人工确认不能把事故标为恢复；手动解除必须提交同资源、晚于失败且成功的运行证据，错误或较早证据失败关闭 |
| D-65 | 已实现并默认关闭，2026-09-15 | 即使别名化，运维域、错误码和聚合状态仍按敏感运维摘要处理；没有单独明确授权时禁止发送外部模型，诊断任务创建前返回412 |
| D-66 | 本机核心已验收，真实模型待授权，2026-09-15 | 运维Agent适配器以别名替换事故/证据ID、去除错误原文和日志，返回后映射本机证据；诊断固定不可执行。GUI/API/CLI/MCP和1280/768已验收，但真实Qwen调用未发生 |
| D-67 | 已验收，2026-09-15 | 完整链路复用正式M1的20条真实Qwen/Spark证据，但必须从原数据库导出精确SQL/哈希/断言，去除日志；不能用参考SQL或只读报告冒充原模型产物 |
| D-68 | 本机已验收，2026-09-15 | 每条完整任务必须生成调度/部署文件、实际Spark演练、摘要审批、本机版本激活、两个墙上时钟Spark批次和健康监控；任一环节缺失即该例失败 |
| D-69 | 本机已验收，2026-09-15 | 正式评测20/20通过、40个上线批次；3次阻塞、1次取消和4次救援原样保留，同任务救援不增加分母 |
| D-70 | 已确认边界，2026-09-15 | 当前100%仅适用于4类客户资产意图×5输入的LOCAL_ACTUAL范围；不替代不同输出契约、权限类场景、公网认证、云隔离或生产E2E |
| D-71 | 本机已验收，2026-09-15 | 公网入口匿名只读，写请求必须有受邀会话、CSRF和项目角色权限；前端按钮状态不能代替后端授权 |
| D-72 | 本机已验收，2026-09-15 | 邀请码随机生成、只显示一次、只存SHA-256、7天到期且兑换后失效；密码使用scrypt参数化哈希，不保存或记录明文 |
| D-73 | 本机已验收，2026-09-15 | 公网会话8小时、HttpOnly/SameSite=Strict/Secure；CSRF使用独立同站Cookie与服务端哈希校验，登录失败每来源15分钟最多5次 |
| D-74 | 本机已验收，2026-09-15 | ADMIN可发邀请；ENGINEER可开发/同步/发布等但不能邀请或改安全策略；PRODUCT_MANAGER聚焦资产/服务/报表；VIEWER无写权限 |
| D-75 | 本机已验收，2026-09-15 | 公网模型Key网页和接口均关闭；回环认证预览可显式允许非Secure Cookie，但该开关在非回环主机启动时失败 |
| D-76 | 已实现并本机验收，2026-09-15 | 云端平台元数据使用MySQL单项目JSON快照与revision CAS；邀请、用户、会话、权限和全部控制面记录可在冷启动后恢复，写请求返回成功前必须完成持久化 |
| D-77 | 已实现并本机验收，真实云待验证，2026-09-15 | 私有OSS以If-None-Match/ETag CAS保存离线落地、实时状态/Checkpoint、DAPI快照和报表快照；格式、行数、主外键与JSON损坏失败关闭，不能用函数内存假冒持久化 |
| D-78 | 试点约束，2026-09-15 | MySQL元数据与OSS业务状态采用“业务状态先、元数据后”的两存储协调，FC最大实例数必须为1；允许留下无引用OSS修订但不允许元数据指向未落盘状态。多实例或生产化前必须改为事务性业务库/提交协议，不能把当前CAS称为分布式事务 |
| D-79 | 已实现、Linux CI待验证，2026-09-15 | V2不使用FC内置Node20冒充兼容：Linux x64包内自带Node24并验证`node:sqlite`；只保留mysql2运行依赖，ZIP设70MiB安全上限。V2用独立函数和手动部署工作流，不覆盖V1入口 |
| D-80 | 文档已实现，2026-09-15 | 公司复刻不迁入本机SQLite/样例调度器，也不推倒成熟中台；复刻对象是统一原子API、受治理Agent闭环、风险分级批准、执行/断言证据和冻结评测方法，底层映射公司已有引擎与治理能力 |
| D-81 | 接口已实现并本机验收，云资源待验证，2026-09-15 | 公网控制面与Spark执行进程分离；远程请求绑定项目/时间/Nonce/正文签名并防重放，Worker白名单化上下文且最多单任务并发。健康检查、HMAC和断言校验不能冒充真实FC私网、Java/PySpark运行时或成本验收 |
| D-82 | 已确认并实现构建门，2026-09-15 | 本机Spark3.5.7证据保持不可变但不部署；新云Worker锁定无PyPI已知漏洞记录的Spark3.5.9/py4j0.10.9.9及下载哈希。云交付精确绑定实际3.5.9和FUNCTION_PROCESS，远程3.5.7失败关闭 |
| D-83 | 本机已验收，2026-09-15 | 项目隔离运行时从Spark3.5.7升级至3.5.9/py4j0.10.9.9，35项真实引擎/交叉口径/测试SQL在新版本全部重跑通过；旧运行记录保留原版本，不批量改写历史 |
| D-84 | 已实现并本机验收，真实OSS待验证，2026-09-15 | 交付包不只保存在元数据：按交付摘要写本机/私有OSS不可变对象，首次create-only，同摘要异内容失败；审批、演练、发布前重新核验正文。旧本机包在首次动作时安全迁移，OSS真实条件写仍待账号验收 |
| D-85 | 已实现并本机验收，2026-09-15 | 云端管理员只允许`V2_BOOTSTRAP_ADMIN_PASSWORD_HASH`，禁止明文密码环境变量；用户密码经本机stdin生成固定参数scrypt哈希，CLI不回显明文。邀请兑换仍只保存哈希，真实密码仅用户掌握 |
| D-86 | 已实现并本机验收，2026-09-15 | 登录后改密要求当前密码、新密码强度和CSRF；成功时撤销该用户全部旧会话、创建新会话并记录无秘密审计。忘记密码恢复不复用引导环境变量，后续单独设计 |
| D-87 | 已实现并本机验收，账号账单待接，2026-09-15 | 默认模型月门50元、远程Spark 200次/3600秒、全站硬上限200元；模型和远程执行在创建/调用前预留额度，超限429且不创建任务。未注入已核验账号支出时页面/API必须显示NOT_CONNECTED，不能宣称全站费用受控 |
| D-88 | 真实账号部分验收，2026-09-15 | 云预检统一用只读CLI并生成0600脱敏JSON；账号/资源/VPC/Bucket/地址/角色和环境值不落报告，只保留状态、规格、存在性、金额与交叉检查。已验证RDS属性/网络、BSS及唯一OSS候选；FC及暂停态库账号保持UNAVAILABLE，写资源仍需另行批准 |
| D-89 | 本机已验收，2026-09-15 | 数据契约V1只能快照当前实际资产字段与证据摘要；后续版本必须来自精确评估。BACKWARD/FULL/NONE策略保守判定新增必填、删除、类型和可空性风险，破坏性变更未经明确确认不得创建版本 |
| D-90 | 本机已验收，2026-09-15 | 契约检查同时绑定契约版本、资产证据、实际元数据、可用实际行、行通过率和新鲜度SLO；无行或无时间证据返回PARTIAL，失败产生告警，只有后续完整PASSED检查可关联解除 |
| D-91 | 本机已验收，2026-09-15 | 契约与资产工作台整合，并由GUI/API/CLI/MCP共享同一资源；资产Agent只接收字段、摘要、检查状态和下游数量，不接收业务行。页面实测兼容V2、违约告警及破坏性V3确认恢复 |
| D-92 | 已配置并只读验收，2026-09-15 | 本机阿里云CLI使用浏览器OAuth和短期STS，不要求用户创建长期AccessKey；配置文件0600，审计器在进程内传递临时凭证并清除。CLI/插件返回不可用仍失败关闭，不降级为明文密钥 |
| D-93 | 真实控制面已验收，数据面待验证，2026-09-15 | 真实RDS中平台元数据使用独立`platform_meta`，不混入`business_demo`；`platform_app`为Normal且只授该库ReadWrite，Describe复核Available。用户密码只经两次隐藏输入和本机进程传递，脚本拒绝覆盖既有账号，不保存或回显密码 |
| D-94 | 本机已验收，2026-09-15 | 调度发布不能因已有GUI/API就声称多端完成；V2 CLI/MCP现共享交付包、文件演练、取消、摘要审批、发布版本、回滚和监控14项原子操作。审批/发布/回滚工具显式要求用户确认，本机发布不改称公网发布 |
| D-95 | 已实现并本机门禁验收，2026-09-15 | V2手动部署工作流不再硬编码账号/VPC/vSwitch/安全组ID或继承旧函数环境；资源标识来自受保护GitHub Environment，函数必须已预创建、同VPC且单并发。匿名公网部署只在专用函数、当前账单、备案/域名与脱敏证据齐备后尝试；现有证据返回拒绝 |
| D-96 | 只读诊断，2026-09-15 | 官方FC 3.0 Node SDK通过OAuth短期STS读取时也返回AccessDenied/服务端称缺SecurityToken；SDK内部确认STS字段存在，不能推断函数不存在或擅自扩大权限。仅保留脱敏错误类别，FC仍为未验收 |
| D-97 | 本机已验收，2026-09-15 | 七阶段Agent证据旅程从版本/运行/交付包/演练/审批/批次/告警当前记录只读推导，不建立第二套模拟状态。代码Agent只覆盖SQL_DEVELOPMENT；工程师交付与批准、调度器发布和监控标明责任，即使本机证据齐全也固定`agentIndependentE2E=false`、`publicDeployed=false` |
| D-98 | 本机已验收，2026-09-15 | 本机Origin校验只接受实际监听端口的回环同源和受限Vite 5173；不把3100写死导致其他安全回环端口白屏，也不因此接受外部域名或公网非HTTPS写入 |
| D-99 | 本机已验收，2026-09-15 | Agent交付准备只接受真实模型与Spark独立五场景通过的源任务；后台复用现有不可变包规范并实际按文件演练，假回执不产生通过，刷新/取消/重启中断有持久状态。模型负责SQL，调度/部署文件由标准生成器产生，不把规则文件冒充模型原创 |
| D-100 | 已确认边界并本机验收，2026-09-15 | 交付准备成功状态为AWAITING_ENGINEER_REVIEW，真实包和演练可核对；已审批源包必须生成新包并重新审阅，不能继承旧批准。审批、发布、受邀公网操作仍由人工/独立权限门控制，`fullLifecycleE2E`、`agentIndependentE2E`和`publicDeployed`均保持false |
| D-101 | 本机已验收，2026-09-15 | 调度页的步骤4和监控只绑定所选包实际发布版本；未审批新包不继承其他ACTIVE_LOCAL历史健康度，不显示其他版本回滚按钮；历史包显示历史状态而非当前生效健康 |
| D-102 | 本机已验收，2026-09-15 | 将工程师审阅从审批备注拆分为不可变交付摘要和成功文件演练编号绑定的独立记录。审阅必须逐项确认代码、断言、交付文件和仅本机范围；审批引用审阅ID，不能绕过或将旧审阅迁给不同摘要。GUI/API/CLI/MCP共享该原子能力 |
| D-103 | 本机已验收，2026-09-15 | 20条完整链路评测升级为七阶段契约；旧摘要审批评测不再计为当前版本。冻结的真实模型SQL必须先在当前Spark3.5.9重跑并通过回归，再验证包、文件演练、四项审阅、审批、计时批次和监控。评测执行者的LOCAL_TEST_RELEASE审阅仅证明技术关口，不能当作受邀用户生产批准或Agent自主完成 |
| D-104 | 本机测试、界面和真实模型跨模块验收，2026-09-15 | 统一Data Agent入口不是新的高权限执行器，而是受治理意图路由：仅可在开发、接入、实时、资产、质量、安全、服务、报表、调度和运维白名单中推荐一个起点及最多三步不重复的模块链路。路由返回NO_EXECUTION，不传业务行/凭证/内部地址；下游专业Agent、资源权限和人工关口不被绕过。真实Qwen已验证“资产理解→报表设计”两步链路。ENGINEER与PRODUCT_MANAGER可提交，VIEWER不可提交 |
| D-105 | 本机API与界面验收，真实云证据待刷新，2026-09-15 | 公网部署前置只向管理员展示脱敏布尔检查、失败项、生成时间和账单金额；函数名、域名、VPC、Bucket、连接地址、账号、密钥及审计原文不通过API或GUI返回。当前审计显示专用FC、目标匹配、同VPC、备案/HTTPS与公网域名匹配尚未验证，部署持续失败关闭 |
| D-106 | 本机角色隔离验收，2026-09-15 | 公网受邀用户的统一Agent意图任务按submittedBy仅供本人读取，ADMIN可读项目审计全量；submittedBy只用于后端过滤，不出现在API响应。PRODUCT_MANAGER具备AGENT调用权限，VIEWER被拒绝模型任务提交 |
| D-107 | 已实现待界面交接验收，2026-09-15 | 用户显式点击跨模块链路步骤时，平台创建不可执行的agent_intent_handoff审计记录；目标必须属于该任务的白名单推荐步骤，成员权限继承意图所有权或ADMIN。交接只预填专业Agent草稿，不创建下游方案、运行、审批、发布或权限变更 |
| D-108 | 已实现待全量验收，2026-09-16 | 统一Agent意图原文只在后台模型执行闭包中临时使用；任务元数据持久化messageHash/messageLength，API/GUI/CLI/MCP不返回原文。明显的凭证、连接串或私钥模式在提交前失败关闭，避免把用户误粘贴的秘密带入模型、元数据或审计 |
| D-109 | 适配器代码与虚构测试验收，真实RDS待验证，2026-09-16 | SERVER_MYSQL只引用服务端环境变量和表白名单，不接受浏览器/CLI/MCP提交的主机、账号、密码或连接串。连接测试只返回状态和版本，元数据只返回字段、行数和结构摘要；首期拒绝MySQL同步执行，待同VPC真实RDS数据面验收后再开放 |
| D-110 | 本机Agent提示验收，2026-09-16 | 离线同步Agent只能把`supportsOfflineSync=true`的本地合成源交给模型；SERVER_MYSQL在真实数据面完成前明确标记为仅连接/元数据，不让Agent生成看似可执行的同步方案 |
| D-111 | 本机真实模型与界面验收，2026-09-16 | 统一Agent为每次模型路由和显式专业交接写顺序轨迹；轨迹只存消息哈希/长度、模块、步数、置信度、目标哈希和NO_EXECUTION，不存原始任务描述或业务行。读取遵守意图所有权/ADMIN边界，轨迹不能证明下游专业任务已执行 |
| D-112 | 云端只读前置完成，FC创建仍关闭，2026-09-16 | 用户授权独立V2 FC但要求预算与最小权限；Cloud Shell复核到2个FC信任角色（1个V2/部署命名候选）、RDS所在VPC有1个交换机和1个安全组、现有函数3个且无V2目标。不能用旧函数、Node14或占位代码冒充V2；真正创建必须同时具备Linux x64 Node24包、专用角色/同VPC绑定、MySQL/OSS/管理员受保护配置和域名门禁。Linux CI已手动通过，但未把完整包上传到额外Artifact，避免扩大代码托管面 |
| D-113 | 工作流已实现，尚未触发云写入，2026-09-16 | 新增独立`provision-v2-staging.yml`，只接受手动触发；先验证当月账单低于200元、函数执行角色FC信任且无管理员级策略文本、vSwitch/安全组同VPC、目标函数明确不存在，再用Linux Node24包执行CreateFunction。若目标已存在或NotFound响应不确定则失败关闭；不删除、不更新旧函数。创建后仍需刷新脱敏审计、真实冷启动和公网部署门 |
| D-114 | 本机配置校验已验收，2026-09-16 | 受保护部署变量与秘密统一由`scripts/verify-v2-staging-config.mjs`校验；只返回缺少的键名和固定错误码，不回显值。HTTPS公网地址、Origin一致性、函数名格式和部署角色/执行角色分离在进入任何云写入前失败关闭 |
| D-115 | 本机完整质量门已验收，2026-09-16 | 最新`npm run ci`为209/209，包含新增V2配置门、工作流静态门和既有模块回归；该数字只证明当前代码/本机合成范围，不升级`publicDeployed`、隔离Spark或公网E2E结论 |
| D-116 | 本机引擎与进程门复验，2026-09-16 | 当前Spark 3.5.9重新执行35/35通过，运行时取消/超时3/3通过；保留`SPARK_AND_ASSERTION_ACCEPTANCE_NOT_MODEL_E2E`和`LOCAL_PROCESS_LIFECYCLE_NOT_CLOUD_ISOLATION`边界，不把本机结果外推到云Worker |
| D-117 | 本机浏览器验收，2026-09-16 | 设置页侧栏覆盖全部经典模块与Data Agent；部署门只显示脱敏状态、账单摘要、待补齐数量和受保护配置入口，浏览器快照未出现账号、地址、资源ID或秘密。该证据仍是本机回环页面，不替代Safari/Chrome公网验收 |
| D-118 | 远程CI已验收，2026-09-16 | 推送后的`codex/v2-foundation`在GitHub Linux Runner上运行CI `35075342577`并成功；该证据覆盖代码/测试/Node24包构建，不证明FC、RDS、OSS或公网已经部署 |
| D-119 | 远程CI快速失败门已验收，2026-09-16 | `provision-v2-staging.yml`的配置校验提前到依赖安装和构包之前；CI `35077273405`在34秒内成功，后续若环境缺项可快速失败，不触发云写入 |
| D-120 | PR已创建、未合并，2026-09-16 | 为让新增预置工作流进入GitHub默认分支，创建不自动合并的PR #59；不在PR阶段触发阿里云写入，合并和v2-staging配置仍由用户控制 |
| D-121 | 配置适配已实现，本机测试通过，2026-09-17 | GitHub `v2-staging` 已创建并写入已核验的9项非秘密变量；由于用户使用的`JASONSECRETS`未出现在GitHub列表，工作流新增白名单JSON/KEY=VALUE bundle展开，逐项Secret优先，值不进入日志。仍不猜测或读取缺失的MySQL密码、管理员哈希、百炼Key和域名 |
| D-122 | 本机完整质量门已验收，2026-09-17 | `JASONSECRETS` bundle展开接入预置/部署两条工作流，新增解析与未知键/多行失败测试；`npm run ci`为212/212。GitHub环境仍无该Secret，云端创建继续失败关闭 |
| D-123 | GitHub非秘密变量已实际写入，2026-09-17 | 创建`v2-staging`环境并写入9项已核验变量（函数、角色、OIDC、VPC、交换机、安全组、OSS和审计文件路径）；未写入任何Secret。增量PR #60尚未合并，域名与Secrets仍由用户控制 |
| D-124 | 域名协作边界确认，2026-09-17 | 域名购买、实名模板、主体备案和支付必须由用户完成；Codex只负责备案后配置Origin/DNS/HTTPS与公网验收。当前无已备案域名，私有函数预置和公网部署严格分开 |
| D-125 | 预置失败关闭与修复，2026-09-18 | `JASONSECRETS` 已存在于GitHub `v2-staging`（只确认名称）；首次预置运行在配置校验阶段失败，未触发依赖安装或阿里云写入。修复入口传递`process.env`并增加CLI真实环境测试，213/213 CI通过；PR #62合并后再重跑 |
| D-126 | 最新云证据，2026-09-19 | PR #59、#60、#62均已合并。预置运行`35298563745`已通过配置、CI、Linux Node24构包、OIDC和当月账单门，随后在`ram:GetRole`被拒绝；`CreateFunction`未执行。先前“仍为BSS失败”和“PR #62待合并”均已过时，不再据此申请额外账单权限 |
| D-127 | 已实现待远程CI，2026-09-19 | 部署秘密除缺项外新增占位符、端口、账号/库名、管理员scrypt哈希和模型Key格式校验；只返回键名/错误码。GitHub Runner在写入临时环境前注册逐值mask，生成文件仍由always清理；这些门不能证明真实秘密有效或云连接成功 |
| D-128 | 已实现待远程CI与云验收，2026-09-19 | OSS对象键改用现有函数运行角色已授权的`data-platform-demo/v2/`隔离前缀；预置和部署要求`instanceConcurrency=1`、`reservedConcurrency=1`及`minInstances=0`，避免把单实例并发误称实例总上限，并允许无请求缩至0 |
| D-129 | 最小权限方案已生成并获授权，2026-09-19 | 部署角色完整策略由脚本从受保护标识生成：RAM读取和PassRole、vSwitch、安全组均精确到单一资源；FC仅包含创建/读取/更新与并发/弹性配置读写，不授予产品通配权限；预算读取继续使用独立`bss:DescribeBillList`。当前授权仅覆盖该精确策略，实际写入与复核证据仍待完成 |
| D-130 | 本机完整质量门已验收，2026-09-19 | 安全门、品牌迁移和持久调度合并后完成`npm run ci`，226/226测试、旧版构建及V2 TypeScript/Vite构建通过；该本机CI仍不证明云函数、公网或真实Secret有效 |
| D-131 | 已实现并本机验收，真实云待验证，2026-09-19 | 云调度不复用FC内`setTimeout`：外部签名tick只领取ACTIVE_CLOUD到期批次，执行前持久化租约，过期租约可恢复，ACTIVE_LOCAL历史不受影响。签名Nonce持久化防重放；GitHub cron只是默认关闭的低成本试点适配器，不作为公司生产调度结论 |
| D-132 | W1真实验收，2026-09-19 | Spark Worker工作流`35427706320`在GitHub Linux/Python3.10成功构建318,451,671字节的Spark3.5.9锁定包并上传1天短期Artifact；不下载到聊天、不提交Git。该证据不包含Java启动、FC私网、真实Spark运行或成本验收；旧Action Node20告警通过升级官方Node24 major版本后再复验 |
| D-133 | 可复现构建证据修正，2026-09-19 | 两次`upload-artifact`摘要不同只证明GitHub外层封装不同，不能直接判定内部Worker ZIP不同。构建脚本仍采用固定时间戳、稳定排序和`zip -X`，工作流单独记录内部ZIP SHA-256；只有两次干净Linux构建的内部摘要一致才验收可复现性 |
| D-134 | W1可复现构建已验收，2026-09-19 | 独立运行`35429097554`与`35429097543`均成功，内部ZIP均为318,395,161字节且SHA-256同为`15c13c30…d2f4a`；外层Artifact摘要不作为构包身份。W1完成不升级W2 Java/FC健康或公网结论 |
| D-135 | 包级执行缺陷已定位，2026-09-19 | 构包后真实启动Worker并执行证券SQL的运行`35430026994`固定失败为`ModuleNotFoundError: sqlglot`；云requirements遗漏执行器实际依赖。按PyPI官方wheel SHA-256补齐sqlglot 27.14.0，修复后必须重新跑真实Spark和可复现摘要，不能沿用旧W1包摘要 |
| D-136 | W1包级运行已验收，2026-09-19 | 修复后运行`35430365703`、`35430441817`均从解压包启动Java17/Python3.10/Spark3.5.9，证券SQL、测试SQL和5套回归全部通过；内部ZIP均318,911,849字节且SHA-256同为`2ba7c6f6…53e0`。W1完成不代表FC私网、函数鉴权、冷启动或费用W2完成 |
| D-137 | 品牌更名已确认，2026-09-19 | 产品中文名统一为“数舵”，英文技术标识统一为`Shuduo/shuduo/SHUDUO`；GUI、CLI、MCP、Cookie、Header、环境变量、协议格式、包名、临时目录及文档同步迁移。当前尚未公网开放，不保留旧品牌技术别名；旧本机会话和旧快照不作为新版本兼容输入 |
| D-138 | 域名已购买、备案待提交，2026-09-19 | 公网目标为`https://shuduoai.cn`，GitHub `v2-staging`的URL与Origin非秘密变量已实际写入。用户反馈需等待至少3天才能提交ICP备案；备案、DNS和HTTPS证据未完成前部署继续失败关闭，私有FC验证可独立推进 |
| D-139 | Agent-first方向已确认，2026-09-19 | 用户所在公司数据中台已建设约8年，当前重点转向Data Agent。数舵Data Agent改为默认一级入口和独立全页面工作台；经典模块保留但仅作为可选专业接管，不再要求用户先选模块或把handoff当成任务完成 |
| D-140 | 第一版实现边界，2026-09-19 | 独立Agent支持任务历史、上下文、1—8步计划、PLAN_ONLY/REQUEST_APPROVAL、会话内启动现有专业Agent及应用草稿；父意图持久记录专业任务ID/类型，历史会话可恢复状态与安全产物。仍不自动审批、发布、授权或恢复；低风险自动推进和统一Artifact协议继续建设 |
| D-141 | 本机真实模型与界面验收，2026-09-19 | 历史跨域任务在独立Agent页面实际启动资产和质量专业Agent，两者使用真实Qwen完成；质量方案经用户点击应用为草稿，资产任务刷新后从持久父子引用恢复并展示安全产物。1440/768无页面级横向溢出、控制台0错误；与最新安全和调度基线合并后完整CI 229/229。该证据仍非公网自治E2E |
| D-142 | 最小权限实现修正，2026-09-20 | 官方授权表确认`DescribeVSwitches`仅支持`vswitch/*`，不能与“精确单交换机”策略同时成立；预置改用支持精确ARN的`vpc:DescribeVSwitchAttributes`并验证ID、VPC和Available状态。新增幂等策略应用器：只创建/附加固定自定义策略，既有同名策略内容不一致时失败关闭，不自动创建新版本或扩大权限。完整CI 233/233；真实RAM写入仍等待已登录Cloud Shell执行与复核 |
| D-143 | 预置快速失败顺序，2026-09-20 | 受保护配置校验后立即完成CLI安装、OIDC换取临时身份以及账单/运行角色/精确网络权限检查，全部通过后才安装依赖、运行完整CI和构包。这样权限或云边界错误不会重复消耗完整构建时间；创建函数仍在质量门与包检查之后，写入范围未扩大。针对性9/9与完整CI 233/233通过，真实工作流待合并后验证 |
| D-144 | CLI不存在判定修正，2026-09-20 | FC 3.0 CLI在404时可能将`FunctionNotFound`写入stderr纯文本，原工作流只读stdout JSON会把安全的“不存在”误判为未知错误。新增脱敏错误码提取器，仅从JSON固定字段或整行`ErrorCode/Code`读取受限字符错误码；未知内容归一为`UNKNOWN_ALIYUN_ERROR`，不打印RequestId、消息、函数名或端点。完整CI 236/236；真实目标不存在检查待云端运行验证 |
| D-145 | 私有冷启动烟测边界，2026-09-20 | 域名备案前的MySQL/OSS冷启动验收使用FC同步Invoke API和专用`PRIVATE_STATUS_V1`事件；服务只在`V2_LOCAL_DEVELOPMENT=false`、`V2_PROVISIONING_ONLY=true`、`V2_PRIVATE_SMOKE_ENABLED=true`三条件同时成立时开放`/invoke`，只返回脱敏健康字段且不触发元数据flush。公开更新环境不包含两个预置开关。部署角色不新增`fc:InvokeFunction`，由已登录Cloud Shell主账号临时调用，避免为自动化扩大到全部FC函数的调用权限。完整CI 238/238；真实调用待函数创建后验证 |
| D-146 | RDS自动暂停冷启动恢复，2026-09-20 | 云端MySQL初始化对`ETIMEDOUT`、`ECONNREFUSED/RESET`、网络不可达、连接丢失、连接数和服务关闭执行最多6次有界线性退避；默认5秒连接超时、2秒基准间隔，总上限约60秒，低于函数120秒超时。鉴权/授权失败不重试；端点和原错误消息不外泄。端口、池、超时、次数和间隔均先做边界校验。完整CI 242/242；真实Serverless RDS唤醒时间仍待私有Invoke实测 |
| D-147 | OSS瞬态恢复与CAS不变式，2026-09-20 | OSS GET/PUT仅对网络错误及429/5xx进行最多3次有界重试，每次重新签名；404、409、412不自动重试。条件写响应丢失后，状态对象只在本次请求确有瞬态重试且远端修订/正文与目标完全一致时收敛为成功；其他陈旧写仍409。不可变产物的create-only重试后读取正文，摘要不同仍拒绝。全部错误脱敏，完整CI 247/247；真实OSS ETag行为待私有Invoke后继续验证 |
| D-148 | SERVER_MYSQL数据面首版，2026-09-20 | 服务端MySQL从“只采元数据”扩展为显式开关下的有界离线同步；凭证、库和表白名单仅来自受保护环境。FULL与全表快照差异UPSERT均真实写独立LandingStore，运行保存计数、水位和摘要但不返回行。Schema变化、行数/字节/类型/超时越界和非白名单失败关闭，错误脱敏。工作台按后端能力显示“仅元数据”或“可同步”，任务下拉过滤不可执行源并将快照差异标为非CDC。当前仅虚构适配器与API验收，真实同VPC `business_demo`及只读账号尚未配置。完整CI 256/256 |
| D-149 | Agent专业步骤批准资源，2026-09-20 | `REQUEST_APPROVAL`不再只是前端选择：后端持久化意图/模块/目标哈希/风险绑定的`agent_intent_approval`，专业任务交接必须引用有效APPROVED记录，绑定后转BOUND并写`STEP_APPROVAL → SPECIALIST_HANDOFF`轨迹。PLAN_ONLY关联已执行任务返回409；非所有者读取批准返回403。GUI在创建专业任务前批准，绑定失败可复用同一任务重试；CLI/MCP共享批准列表/创建原子API。完整CI 257/257，仍不自动应用草稿、运行、发布或授权 |

## 替代关系
旧菜单、关键词路由、固定评分、模拟发布和静态权限标签不构成 V2 验收证据。
旧版保留作对照，访问审批未完成改动留在原分支，不混入 V2。
