# V2 验收与运行证据

日期：2026-09-18。分支 codex/v2-config-fix，基于已合并 PR #60 的 main。
当前交付覆盖 M0 基线、M1冻结范围、M2a文件交付以及M2b/M2c本机标杆链路，**不是 M0–M5 全部完成，也没有替换公网产品**。

## 自动检查

| 检查 | 结果 | 证据与范围 |
|---|---|---|
| npm run ci | 最新221/221测试通过，源码检查、旧版构建、V2 TypeScript/Vite构建通过 | 包含受保护部署配置、持久调度租约/签名/replay门、Spark Worker确定性构包门和既有全量V2回归；不代表公网云资源已部署 |
| npm run v2:spark-test | 最新 35/35 通过 | 真正Apache Spark 3.5.9；包含五场景、测试SQL真实执行、失败定位与假通过拦截；本轮约22.026秒。历史3.5.7证据未改写 |
| npm run v2:runtime-test | 3/3 通过 | 提交前取消、超时终止子进程、运行中取消；非云端隔离证明 |
| npm audit | 0 项已知漏洞 | Vite 更新为安全公告推荐的 7.3.6 后复查；不是绝对安全证明 |
| git diff --check | 通过 | 无差异空白错误 |

边界测试使用 TEST_DOUBLE 测模型 API 协议、三次修正上限和故障状态，不能算真实模型任务完成。
本报告原始验收时真实模型调用次数为 0。≥85% 指从需求理解到上线后监控的完整 E2E，尚未评测；上述基础、引擎和进程测试都不能替代它。
2026-09-14 已完成真实模型试跑，最新详情见 [M1 真实试跑报告](m1-live-agent-report.md)，不能继续以原始“0次调用”描述当前状态。

Spark 用例涵盖：标准资产、现金变化、重复持仓、证券去重、现金客户、缺少现金、空值、金额精度、跨顾问范围、日期过滤、重复归属、多条现金；
错误结果不能算成功；未知字段由引擎报错；DROP、多语句、未登记表、外部文件和反射调用被拒绝。
首次发现校验器将 Spark CAST 解析为 TRY_CAST 并误拒绝；修正白名单后，全部重新执行通过。

## 浏览器真实链路

验收浏览器为 Codex 内置浏览器；没有以此冒充 Safari/Chrome 的公网验收。

| 场景 | 实际证据 |
|---|---|
| 保存和运行 | 按钮创建代码版本，提交后台 Spark；页面显示排队、运行与完成 |
| 标准客户资产 | 运行 e20e39bb-7c0e-474b-979b-f0894e4d00c8；7,202ms；CLIENT-001：1500 + 300 = 1800 |
| 现金变更 | 运行 e8e4a661-2655-4d2e-9048-c66d9fd5803b；8,592ms；CLIENT-001：1500 + 800 = 2300 |
| 版本绑定 | 上述运行均绑定 SQL SHA-256：e5efab3c7e75132e2f30ed6e7f88a3c6adc0fa38f7a5a779c0e6548e7f225004；上下文不同，结果不同 |
| 刷新恢复 | 刷新后恢复 3 个已保存版本、最近上下文及真实 2300.00 结果；未重新提交运行 |
| 编辑与差异 | 编辑器接收修改，差异视图展示变更；撤销恢复载入 SQL |
| 旧结果提示 | 编辑后结果区变为“历史结果”，显示“代码或上下文已变更，请重新运行核验”；撤销后版本重新匹配 |
| 验证包 | 真实运行 bundle 接口返回 200，SQL 哈希一致、客户总资产 2300.00、validation=true、NOT_PUBLISHED |
| 模块搜索 | 搜索 DAPI 定位数据服务；明确显示 M3 范围，不伪装为已发布接口 |
| 响应式 | 1440×900、1280×800、1024×768、768×900，document.scrollWidth 与可视宽度一致；窄屏 Agent 可展开/收起 |
| 模型未配置 | 显示未连接并禁用委托；平台设置提供专用密码输入框和官方 Key 页面链接 |
| 配置直达 | /v2/?module=settings 可直接进入平台设置，密钥输入类型为 password；表单未填入真实密钥 |

代码结果可以在表格区域内滚动；不以页面溢出来容纳更多模块。
主要正文字号和字段文字对比度已调整；全部状态的 WCAG/屏幕阅读器自动审计尚未完成。
版本弹窗已实现焦点约束与 Escape 支持，完整键盘用例仍需扩充。

## 本地凭证防护

设置接口只在本地开发模式开放，密钥写入项目内权限 0600 的 .env.local，不回显、不保存到任务/审计内容、不提交 Git。
换行注入、符号链接目标和公开模式请求的拒绝均有自动测试。
凭证保存功能测试只用虚构密钥；后续真实模型试跑由后端使用用户保存的密钥鉴权，不回显到对话或报告。保存动作本身不自动调用模型。

## 已生成的本地证据

- .v2-artifacts/spark-acceptance.json：最新 35 个实际引擎/交叉核验/测试SQL用例。
- .v2-artifacts/runtime-acceptance.json：进程生命周期验收。
- .data/v2-platform.sqlite：版本、后台批次、结果；设置操作发生后记录不含密钥的审计。
- .v2-artifacts/：各运行输入/输出和临时工作目录，均为合成数据且已忽略。

生成物不提交 Git；复现方式见 [快速启动](v2-quickstart.md)，接口见 [V2 API](v2-api.md)。
源码与这些说明会保留，不依赖截图代替测试。

## 尚未完成的门槛

1. 当前冻结范围的完整E2E已完成20/20、100%，但仅覆盖4类客户资产意图×5输入；不同指标、输出契约、权限失败和跨模块Agent任务仍需作为下一轮更广评测，不能外推100%。
2. 受邀认证与项目角色已在本机/public-preview模式验收；MySQL/OSS持久化代码及模拟冷启动已验收。HTTPS公网Cookie、真实云连接、跨用户并发、隔离执行和多实例架构仍待验证。
3. M2a/M2b/M2c及M3 DAPI/XAPI的云端/公网版本；当前V2 CLI/MCP只证明本机同源，不证明公网身份与隔离。
4. M4剩余经典模块的真实手动流程、API、Agent验收；M4a—M4f已完成本机范围，M4g平台运维核心完成但真实模型诊断因隐私外发门待单独授权。
5. 当前 FC/OSS/RDS 账单和试用到期成本、域名实名/备案、月度预算硬限制。
6. 国内两类网络、Safari/Chrome 首屏可操作 ≤3秒及冷启动对照。
7. 最终全流程 PPT 与公司适配复刻材料。

旧分支中的访问审批改动、用户 PPT 和私有材料保持原样，本轮未推送或部署。

## 2026-09-15：公司复刻手册V1

- 将当前产品定级为L2可信单机场景，目标先到L3团队试点，再评估L4企业平台；没有用菜单数或宣传材料声称超过世界级厂商。
- 结合2026-09-15官方资料刷新DataWorks、Databricks Genie Code、Snowflake Cortex Agents、Microsoft Fabric、Collibra和Informatica能力信号及预览/权限边界。
- 形成公司已有成熟中台的适配映射、产品经理与Codex/研发两条路线、R0—R7里程碑、首条财富顾问持仓旅程、风险分级和评测指标。
- 明确优先复用公司现有身份、引擎、调度、元数据、审批和监控，不把本项目本机SQLite/样例调度器作为生产迁移方案。
- 正式全流程PPT仍未生成，遵守用户约定：在最终范围完成或用户明确要求时再生成。详见[公司复刻手册](company-replication-playbook.md)和[全球Gap路线](second-goal-gap-and-roadmap.md)。

## 2026-09-13 补充：完整 E2E 与分阶段交付

- 用户确认 E2E 必须覆盖需求理解、代码、调试、调度文件、部署文件、上线、上线后监控，并允许分阶段实现。
- PRD 2.1、决策 D-21/22、M1/M2a/M2b/M2c 路线已同步；未把阶段通过率挪用为完整 ≥85%。
- 当前 Agent 成功任务明确返回 completionScope=SQL_DEVELOPMENT、fullLifecycleE2E=false；新增测试验证这一边界。
- 工作台增加“当前仅代码阶段；完整 E2E 还需调度、部署、上线与监控”提示，浏览器已核对。
- 用户反馈已保存密钥，但只读核验时服务仍为未配置、项目配置文件不存在；页面显示格式校验拒绝。没有读取密码框内容，也不把“点击保存”当作已生效。
- 修复复制首尾空白时的误拒绝，仍拒绝内部空白/换行注入和符号链接；保存错误在按钮旁持续显示。
- 补充后 npm run ci：48/48 通过（旧版 28 + V2 20），构建通过；没有为这次文档/保存变更重复计算 Spark 的 23 项用例。

## 2026-09-14：保存 Key 的兼容性与反馈修复

- 用户截图显示请求已返回“未保存”，不是按钮完全未响应。没有读取原页面密码框；不能从掩码截图确定其真实内容。
- 发现实现缺陷：旧校验仅允许总长 23–243 且只接受字母、数字、下划线和短横线，会误拒长/分段凭证；不同失败原因共用一个泛化提示。
- 按传输/存储安全边界修复长 Key、分隔/编码字符和外围普通引号处理；保留内部空白/环境注入/掩码/错误凭证类型拦截。
- 保存错误分配独立错误码，消息不回显密钥；已存在的凭证不会被无效输入覆盖。
- 新界面显示“正在保存…”和“已保存，连接未验证”，请求有超时及中文错误反馈；空输入可以提交并得到明确提示，不再无说明禁用。
- npm run ci：54/54 通过（旧版 28 + V2 26），TypeScript/Vite 构建通过。长测试 Key 可原样保存并经 Node dotenv 解析恢复，文件仍为 0600。
- 独立浏览器测试页提交虚构脱敏值，按钮旁显示具体“脱敏展示值”原因，按钮恢复可操作。该请求返回 400，未创建任何配置文件、未调用模型。
- 用户原输入页没有刷新、未读取或替换；修复已加载到本机服务，旧页面再次提交也能使用新的后端校验。没有宣称用户真实密钥已验证有效。

## 2026-09-14：真实 Agent 代码阶段已跑通

- 用户保存后，本机配置加载成功；真实模型返回 SQL 与 usage，认证已由实际调用验证。
- 首次试跑暴露 AND/OR 保护器误拒，修复后仍保留全部失败记录。
- 三场景试跑的 SQL 虽通过当时样例，人工复核发现漏掉两类业务边界；升级为五场景并为验证范围设置版本，旧报告显示需重验。
- 最新任务 9cc8bf9c-a1a2-426f-9065-538ae86fe951 首次触发真实字段报错，第二次由模型自动修复，通过全部五套数据；任务总时长35.599秒。
- 当前通过 SQL/运行与五场景报告一致，验证包 requiresRevalidation=false；仍为 NOT_PUBLISHED / SQL_DEVELOPMENT，不能计入完整 E2E。
- 浏览器“审阅生成代码”可关联到对应运行；“验证”页实际展示五套场景通过和版本 b7a73bf0、批次 3ad518cd，未用静态成功图代替。
- 全部三次诊断试跑共8次模型请求，标价估算约0.08052元，具体用量、失败原因和产物哈希见真实试跑报告。
- 此处记录最初三次诊断后的状态；随后M1冻结20例及M2a/M2b/M2c本机标杆链路已经完成，范围以本文件后续章节和路线图为准。

## 2026-09-14：M2a本机文件交付子能力

- 新增规范化交付包、固定DAG、样例T+1日历、文件完整性检查、CLI、后台演练记录和工作台。
- UI生成包4bca4494-5173-450a-91ce-af29d48cdbd0，源代码为已通过五场景核验的 b7a73bf0 版本；未用假源码或预设结果冒充。
- API/UI演练 aa4eb81b-8ff5-4f50-8e8c-bab2d6649025：Spark3.5.7实际执行main.sql和tests.sql，通过五场景；约9.317秒。
- CLI从同一落盘目录复现成功，退出码0，约12.167秒；测试SQL返回唯一布尔true，与其文件摘要一致。
- 交付包下载到本机后，经真实文件校验通过，大小34535字节、8个内容文件，摘要9faab3af5c41725bc4f6aafb30ffe7b3293d123cc0266c1dba81c9b1bdf251c2。浏览器下载事件监听未收到通知，但实际下载文件存在且校验一致，因此以文件证据为准。
- 刷新恢复了已有包和演练结果。1280宽时主按钮位于首屏；768宽时页面scrollWidth与可视宽度一致，未出现主体横向溢出。
- 测试覆盖拒绝过期/失败源证据、篡改、重算内部摘要、错误DAG、未知云目标、路径与符号链接、非交易日、重复提交、测试SQL不通过。测试SQL报错时保留主SQL已执行证据。
- published=false、schedulerTriggered=false、fullLifecycleE2E=false。真实定时触发、审批、上线和监控未实现；M1更广泛评测仍保留，不因准备M2a而标为完成。
- 本轮没有模型调用和新增收费云资源。详细文件作用、CLI命令和验收边界见 [M2a规范](m2a-delivery.md)。

## 2026-09-14：M1冻结20例代码阶段评测

- 评测集：4类开发意图×5套合成数据，共20例；摘要`c97cce1…a7e`。每例最多3次修正，真实模型输出必须由Spark与全部5套独立回归验证。
- 首轮`34c20547-6760-4def-9840-e8f442028106`为4/20；其中16例因评测幂等键超过接口上限，未进入模型。缺陷修复为固定长度摘要键，旧报告保留。
- 同一冻结集正式复跑`a6ccf750-836d-4f1b-8e73-a66da710e1bb`为20/20，33次模型尝试、37,559 Token；失败0、未执行0。
- 结果只证明当前客户资产SQL开发范围，`completionScope=SQL_DEVELOPMENT`、`fullLifecycleE2E=false`。详见[M1报告](m1-live-agent-report.md)。

## 2026-09-14：M2b/M2c本机真实发布与恢复

- 健康基线发布`23969ed1-9330-4b19-8ba3-bed83831962f`绑定审批`b84c26eb-9e12-4ffd-b6cd-65fc0603ca67`和包摘要`9faab3af…51c2`。
- 墙上时钟实际触发批次`d5753440-19fd-4820-a780-88da5c8fe3ee`、`e6829379-5f59-4a0c-bcff-458b5532f9fa`；Spark3.5.7分别9,016ms、8,361ms，tests.sql与5套回归通过，触发误差约2—3ms。
- 受控完整性故障版本`106c919b-b71c-4cf1-abce-3d069a3ee9c3`有1个先行成功批次及2个摘要不匹配失败批次；失败记录均由墙上时钟触发，并生成2个开放告警，健康度变为CRITICAL。
- 恢复发布副本的可信摘要后，回滚`98c6c913-be95-492f-b248-64bde29594fa`切换到最近健康版本，解除2个告警；计时恢复批次`14dab66b-7f5c-463a-ba18-6f61f63b5e70`和`7b1e764d-b655-4c14-a315-becf000a6123`真实Spark执行成功。
- 最终当前本机版本HEALTHY，全局实际成功8、失败2、开放告警0、已恢复告警2；失败未删除。所有记录仍为`publicDeployed=false`、`fullLifecycleE2E=false`。
- 浏览器实测1280×900与768×900页面级无横向溢出；发现768双卡过窄后改为单栏，复查健康度与按钮完整，控制台无错误。
- 详细边界、ID和时间见[M2b/M2c报告](m2b-m2c-local-release.md)。本轮没有创建云资源或调用公司数据。

## 2026-09-14：M3数据服务进行中

- 新增独立业务SQLite、DAPI/XAPI逻辑资源和不可变版本、实际查询测试、发布/切换、应用授权、持久化限流、超时、OpenAPI及调用日志。
- 两条DAPI和一条XAPI已从真实Spark发布批次生成并发布；示例外部调用得到CLIENT-001持仓1500.00/证券2，CLIENT-002持仓750.00/现金200.00/总资产950.00。
- 完整令牌只写入受限临时文件用于两次验收调用，随后文件删除、应用撤销；平台列表不返回令牌或哈希。
- 真实Data Agent使用1408 Token生成受治理XAPI方案，后端校验精确DAPI版本后人工应用为草稿，未自动测试、发布或创建凭证。
- 新V2 CLI和MCP实际读取同一服务/版本/OpenAPI资源；旧V1 CLI/MCP不作为证据。
- 16项M3/多端针对性测试及全量CI 92/92通过；实际版本/限流和浏览器响应式证据均已补齐。详见[M3报告](m3-data-services.md)。
- 持仓DAPI V2实际发布后，前两次应用调用成功，第三次返回HTTP429并记录RATE_LIMITED；随后切回稳定V1、撤销临时应用并删除明文令牌文件。
- 数据服务页在有效1280/768视口均无页面级横向溢出；XAPI选择、OpenAPI展开和实际查询按钮通过，控制台无错误。查询定义字面换行缺陷已修复并复查。
- 因此M3仅在本机范围标为已验收；公网、云端身份、分布式限流和生产秘密管理仍未完成。

## 2026-09-14：M4a接入与离线同步进行中

- 新增真实CSV解析、连接测试、字段类型/可空/基数扫描、元数据版本差异、独立SQLite落地区、FULL与INCREMENTAL_UPSERT。
- 本机实际全量运行读取/插入4行；增量运行读取2行、插入1、更新1、最终5行，POS-002市值由500.00更新为550.00，新增POS-005。
- 新源版本增加currency字段，元数据准确记录added=[currency]；旧任务重跑返回409并保存失败运行，未覆盖目标表。
- 13项接入/同步/Agent针对性测试通过，多端契约同步扩展，全量CI为105/105。详见[M4a报告](m4a-ingestion.md)。
- 真实同步Agent使用912 Token生成增量UPSERT方案，精确绑定源/元数据版本；人工应用为READY任务且运行数0。
- V2 CLI/MCP均读取实际源和5行落地结果；浏览器有效1280/768无页面级溢出，结构变化、任务选择及实际重跑交互通过，控制台无错误。
- 浏览器重跑增量任务读取2、0新增/0更新/2未变，目标摘要保持一致。因此M4a仅在本机合成CSV范围标记已验收。

## 2026-09-14：M4b实时同步本机验收

- 新增本机事件日志、严格行情事件契约、事件幂等、最新状态、Watermark、Checkpoint、延迟/吞吐、停止与中断恢复。
- 实际首次运行在受控负价格offset2失败，保留2事件和offset1 Checkpoint；修正版本从offset2恢复，最终5唯一事件、1重复、3只证券、3个Checkpoint。
- 失败告警已关联恢复运行并解除；监控明确Kafka/Flink未连接、不是公网部署。
- 真实Qwen使用508 Token生成实时方案，精确绑定现有源版本；人工应用只创建READY任务且运行数0，没有越权启动。
- 通过工作台显式启动Agent草稿，运行`d445ee9a-8837-4476-8595-88d8f3ceb991`在265ms内处理5个唯一事件、识别1个重复并保存3个Checkpoint，实际状态与表格一致。
- V2 CLI/MCP读取同一streams监控资源；11项实时/Agent针对性测试与5项接口回归组合为16/16，全量CI为116/116。
- 浏览器有效1280与768宽度均满足document.scrollWidth=innerWidth；Agent方案、启动、状态、Checkpoint与运行证据可操作，不把Kafka/Flink或公网显示为已连接。详见[M4b报告](m4b-realtime.md)。

## 2026-09-14：M4c数据资产本机验收

- 目录从当前Spark样例、已采集源、离线落地、实时状态、发布数据集及DAPI/XAPI自动生成15个逻辑资产；多次成功发布批次合并为一个业务数据集，底层批次仍作证据保留。
- `landing:raw_positions`当前5行、7字段；血缘仅显示两个直接源版本到该表的FULL/INCREMENTAL_UPSERT关系及7字段映射，不再把共享源的兄弟表误纳入链路。
- 资产说明V1把该表标为“证券持仓明细”；底层摘要`58f29764…567c`不因业务说明变化而覆盖。
- 指标`b3f170f9-df9a-402f-be73-a5c2d1dd12f6`在5行实际执行：股票10750.00、基金300.00、债券550.00，结果摘要`1b1c19d7…90d1`；金额按整数分计算。
- 标准`e17f84b8-83a5-4b7b-adc0-4e8748468176`对security_code实际检查5行、5/5通过；失败设计只返回值哈希。
- 真实资产Agent任务`702082e6-d094-4a64-a5c5-a03968a52dbc`使用3040 Token，引用一个发布数据集、3张输入表和4个下游DAPI/XAPI；回答明确版本绑定血缘与本机边界。
- GUI实际重跑指标与标准，页面显示同一结果；V2 CLI/MCP实际读取同一资产列表，针对性与全量CI为121/121。有效1280/768均满足document.scrollWidth=innerWidth。详见[M4c报告](m4c-assets.md)。

## 2026-09-14：M4d数据质量本机验收

- 规则`ff2971c2-0f72-4d80-9685-63cadf5130bc`绑定`landing:raw_positions`证据摘要和market_value字段；V1范围0.00—5000.00实际检查5行，4/5通过。
- 失败运行`ed68dd2e-dc95-4cf9-bc53-96e79bf08c25`只保存一条无效行SHA-256，不回显持仓；告警`23cdbd72-675a-4c16-9eb9-c02f8950dc3d`保持OPEN。
- V2范围校准为0.00—10000.00，恢复运行`8cd21602-4f19-41ea-a006-1f96fe05cbc0`为5/5通过，原告警以该运行ID解除；V1及失败运行仍可见。浏览器再次实际检测生成`b29963a3-9000-43dd-bc8a-e378a1c1b99a`，仍为5/5。
- 真实Qwen质量方案`2c3ce4a1-7fad-4488-b40a-ad2aad76f1c2`使用1046 Token，后端校验为security_code NOT_NULL；人工应用为规则`3ee8c75b-94e8-4386-b41e-55a8edb22fab`，运行数0。
- GUI/API/CLI/MCP覆盖概览、规则、版本、执行、告警、恢复和Agent；5项新增质量测试使全量CI为126/126。有效1280/768均满足document.scrollWidth=innerWidth。详见[M4d报告](m4d-quality.md)。

## 2026-09-15：M4e安全与脱敏本机验收

- 策略`a718a345-3e62-469d-ac5d-a4a0c5b12e44`绑定`landing:raw_positions`及V1摘要`2eb5389d…8664`；财富顾问使用ADVISOR_CLIENTS范围，未声明字段默认DENY。
- 实际安全查询从5行只返回名下4行，`position_id=******`、`client_id=CLI***001/002`，不包含CLIENT-003；审计`11942f0e-fc79-4df6-9eb4-ad895677f255`不保存业务行。
- 虚构审计员首次查询403并留审计；申请`3c834e31-5156-4a06-83cc-fd3ab4d6d883`经虚构数据负责人批准，授权`8a9b1967-4b9a-4e83-b8b3-47a666840ca0`有效24小时，随后返回5行且两标识字段仍脱敏。
- 真实Qwen安全方案`7e362879-ed9a-4768-858a-41d9520c141d`使用1103 Token；后端验证DATA_ENGINEER、ALL范围和7个字段动作，人工应用为策略`c3eb9b93-a406-4d73-b023-e781e0923f88`，未执行查询。
- GUI实际执行顾问查询，又提交并审批一条临时授权；V2 CLI/MCP共享actor头和安全资源。5项新增安全测试使全量CI为131/131，有效1280/768均满足document.scrollWidth=innerWidth。详见[M4e报告](m4e-security.md)。

## 2026-09-15：M4f数据报表本机验收

- 持仓数据集`7d40386e-092e-45d4-b577-17c327cd96f2`实际物化5行到独立报表库，平台快照`66895802-5084-44e9-877b-464de2ecaf89`绑定资产摘要`58f29764…567c`和内容摘要`60354f99…be8b`。
- 报表`abcd2d24-3726-4d38-919f-4ce1b20da03a`运行`302bf8e3-c34f-4e11-a03d-203d17912d48`实际得到持仓市值11600.00、证券数量4、资产类别3组、行业4组；浏览器重跑保持相同摘要。
- 客户资产数据集从已验证发布结果物化2行；报表`2e7d71f2-da2e-4e77-af52-f84ca004773f`运行`d639a1e8-2670-4d8b-9ada-793029af5d26`得到总资产2750.00，客户分布只显示CLI***001/002。
- GUI实际显示KPI、饼图和柱状图并导出聚合CSV；页面不出现明文客户号，导出不含客户/持仓明细行。
- 真实Qwen报表方案`fc3fd3a3-e0f1-4913-8379-33b825001ba0`使用1258 Token，精确引用持仓数据集快照与4个组件；人工应用为报表`1d803c8b-b63d-41a4-9254-16aca76d3ca7`，运行数0。
- GUI/API/CLI/MCP覆盖数据集、快照、报表版本、运行、导出和Agent；6项新增报表测试使全量CI为137/137，有效1280/768均满足document.scrollWidth=innerWidth。详见[M4f报告](m4f-reports.md)。

## 2026-09-15：M4g跨模块运维核心验收

- 显式扫描42条实际活动，按失败源运行幂等生成5个事故：离线同步1个OPEN，实时1个RESOLVED，质量1个RESOLVED，发布2个RESOLVED。
- 实时事故`abc4f92c-de1b-46e4-8bc8-2709cc553dc5`从失败`82e2e00d…`关联恢复`0d66632e…`；质量事故关联规则恢复`8cd21602…`；两条发布事故关联回滚批次`7b1e764d…`。
- 单元/API验证ACKNOWLEDGED不等于恢复；错误资源证据被拒绝，只有同任务、晚于失败的SUCCEEDED运行可以解除事故。
- 两次真实Qwen运维诊断在外发前被安全策略拒绝。别名化后仍认为运维域/错误码/计数需要单独授权，因此新增默认关闭的`V2_ALLOW_EXTERNAL_OPS_CONTEXT`隐私门；页面明确提示且诊断按钮禁用，未创建真实模型任务。
- GUI实际刷新事故并查看实时恢复链；V2 CLI/MCP覆盖概览、刷新、事故、确认、证据恢复和诊断接口。6项新增测试使全量CI为143/143，有效1280/768均满足document.scrollWidth=innerWidth。详见[M4g报告](m4g-operations.md)。

## 2026-09-15：20条本机完整E2E正式评测

- 先从原正式M1临时数据库导出20条精确SQL、代码哈希、原Agent任务/尝试/Token、Spark3.5.7结果和五场景断言；冻结清单不含日志和本机路径，源报告摘要`8da9a47f…a32b`。
- 正式运行`5db021f8-2efd-4599-848e-25fe29611559`逐条完成需求证据、代码/调试、调度文件、部署文件、实际Spark文件演练、摘要审批、本机版本激活、两个墙上时钟Spark批次及健康监控。
- 完成20/20、成功20、失败0、未执行0，本机完整链路率100%，超过≥85%门槛；总上线批次40个。
- 原样保留3次BLOCKED（非交易日、错误摘要、幂等冲突）、1次CANCELLED文件演练和4次救援；它们没有增加分母或被删除。
- 报告接口、V2 CLI/MCP与运维页显示同一20/20、40批、阻塞/取消/救援信息；有效1280/768无页面横向溢出。
- 该结论固定`deploymentScope=LOCAL_ACTUAL`、`publicDeployed=false`；仍不是公网、云隔离或生产E2E，且用例输出契约集中于客户资产。详见[完整链路报告](full-lifecycle-evaluation.md)。

### 当前审阅契约复跑

- 上述旧运行未包含独立`delivery_review`，在D-102后不再作为当前完整E2E依据。当前运行`f3e1e908-3d27-4622-a2cb-90b246f827bb`按七阶段契约重跑20/20：每例原冻结真实模型SQL先在当前Spark3.5.9实际执行、通过五场景回归，再生成包、文件演练、写入摘要/演练绑定的四项审阅记录、审批、两个墙上时钟批次和健康监控。
- 当前报告：20成功、0失败、40调度批次、`targetMet=true`、`publicDeployed=false`；CLI读取同一报告并确认20条`engineerReview=PASSED`。评测器的`LOCAL_TEST_RELEASE`审阅用于验证不可绕过的技术流程，不是受邀用户的生产批准，也不构成Agent自主成功率。
- 运维监控页实际展示“需求 → 代码调试 → 调度部署 → 审阅 → 上线 → 监控”、20/20、100%、40批及本机/公网边界；不再沿用旧版六阶段“摘要审批”文案。

## 2026-09-15：统一 Data Agent 受治理入口

- 新增独立“智能协同 / Data Agent”页：一个任务输入可在数据开发、接入、实时、资产、质量、安全、服务、报表、调度、运维十个白名单模块中选择一个推荐起点及最多三步不重复的模块链路；页面明确显示风险等级、推荐理由、置信度和“默认不执行”。
- API任务固定`completionScope=CROSS_MODULE_INTENT_ROUTING`、`execution=NO_EXECUTION`。模型仅接收用户任务文字与无业务数据的模块目录；后端验证模块白名单、摘要/理由/置信度，丢弃模型附带的SQL等未声明字段，不创建下游方案、查询、同步、审批、发布或令牌。
- ENGINEER与PRODUCT_MANAGER拥有`AGENT`权限；PUBLIC匿名读取意图记录返回401。GUI/API/CLI/MCP均调用`/agent/intents`，CLI和MCP不将令牌置入参数。
- 使用测试替身实际完成报表路由，核验未创建`report_agent_plan`或报表；浏览器实测显示10张专业模块卡、默认不执行边界，1280宽度无页面级横向溢出。为避免消耗不必要模型额度，本轮未提交真实模型路由，仍待单独的真实模型验证。
- 公网角色隔离：PRODUCT_MANAGER可提交并只读自己任务；VIEWER提交返回403；ADMIN可审计全量。后端用提交者内部标识过滤但从响应剥离，避免跨成员暴露需求或内部ID。
- 新增可审计交接：当前意图所有者或ADMIN可将任务交接到模型已经推荐的步骤；交接固定`NO_EXECUTION`，记录目标与目标说明但不返回提交者内部标识。非所有者读取交接返回403，且交接后未创建下游报表/同步等资源。
- 新提交的跨模块任务不在元数据或响应中保存原始描述，只保存长度和SHA-256；密钥、密码、令牌、JDBC连接串和私钥模式被422拒绝。模型仅在后台任务执行时临时接收通过校验的描述。

## 2026-09-16：服务端 MySQL 数据源受限适配

- `SERVER_MYSQL`源仅可选服务端白名单表，凭证只读取服务器环境变量；API、CLI、MCP和GUI不接受主机、账号、密码、Token或连接串。未配置时GUI明确禁用MySQL类型，不引导用户粘贴凭证。
- 虚构适配器实际验证连接探测返回状态/版本、元数据采集只返回字段/行数/结构摘要；浏览器凭证和非白名单表失败关闭，且对该源创建离线同步任务返回`MYSQL_SYNC_NOT_ENABLED`。
- 此处是连接器代码与虚构测试验收，不是已连接真实RDS；真实同VPC、最小权限账号、冷启动和合成表数据面验证仍在公网前置之后执行。
- 离线同步Agent输入目录同步增加`supportsOfflineSync`能力标记，并在模型提示中明确排除SERVER_MYSQL；定向测试确认只传元数据不传CSV行，防止未验收的云连接被误生成为可执行同步。

### 真实模型单模块路由验收

- 随后以“虚构客户持仓字段解释与资产分析报表口径”提交真实Qwen路由任务`1d582304…`。模型使用487 Token，推荐“数据资产与契约”，置信度95%；任务固定`NO_EXECUTION`，未创建下游查询、报表、同步、审批或发布，且不计完整E2E或公网部署。
- 此结果证明真实模型可在白名单内形成受治理单模块建议，也说明“字段解释 + 报表设计”这类复合需求不应被压缩成一个模块。因此契约升级为最多三步链路；跨模块真实模型验收仍待下一条独立虚构需求，不以现有单模块结果冒充完成。

### 真实模型跨模块链路验收

- 以“先理解虚构持仓字段、血缘和总资产口径，再生成资产类别/行业分布报表”提交任务`21b2707d…`。真实Qwen使用610 Token，返回两步不重复链路：`数据资产与契约 → 数据报表`，第一步解释字段/血缘/口径，第二步设计报表草稿；置信度90%。
- 页面实际展示两步顺序、每步风险和目标，1280宽度无页面级横向溢出。任务持续固定`NO_EXECUTION`、`fullLifecycleE2E=false`、`publicDeployed=false`，没有生成下游数据资产Agent任务、报表草稿、查询、审批或发布。
- 交接实测：点击第二步仅将“基于确认口径设计资产类别和行业分布分析报表草稿”带入报表专业Agent输入框，并显示“请在模块内审阅后再发起”；未自动提交报表方案、运行、导出或发布。资产与报表两模块均保留自身资源、快照和审批边界。
- 路由轨迹实测：真实任务`81037f6f…`通过CLI读取到一条`MODEL_ROUTE`，目标为质量模块、2步、置信度90%、`NO_EXECUTION`；统一入口页面同步显示“模型路由”轨迹。轨迹未包含原始需求、业务行或凭证。

## 2026-09-15：公网部署前置脱敏看板

- 新增`GET /cloud/readiness`和平台设置看板，复用部署工作流的同一云前置判定。响应只含就绪布尔、检查项、失败项、审计生成时间和账单税前金额；合成测试确认函数名、域名、Bucket等资源标识不在响应中。
- 当前已读取的脱敏证据通过格式、地区、账单、200元硬门、独立MySQL账号和私有OSS检查；仍失败关闭：专用函数、目标哈希、同VPC、备案/HTTPS、域名匹配。页面提供Cloud Shell直达入口与刷新，不提供敏感资源输入框。
- 2026-09-16 Cloud Shell复核：STS_OK；FC可读函数3个但V2命名候选0、VPC配置1个、单并发0个；RDS最终已停止且AutoPause=true、Private端点1个；授权唤醒窗口内实际确认2个数据库、2个账号，`platform_app`对`platform_meta`为ReadWrite，随后执行Stop并确认STOPPED；账单税前约¥0.60/未付约¥0.60；OSS可见1个杭州Standard/private/LRS候选。专用FC/同VPC/备案仍不成立。
- 本机测试覆盖“完整证据返回就绪且不泄露资源标识”；真实云只读审计仍需在Cloud Shell刷新后才可改变结论。公网匿名读取该看板返回认证拒绝；本机模式用于当前验收。

## 2026-09-15：邀请认证与项目权限本机预览

- 后端实现一次性7天邀请、scrypt密码哈希、8小时服务端会话、HttpOnly/SameSite Cookie、CSRF双提交、登录限流和ADMIN/ENGINEER/PRODUCT_MANAGER/VIEWER资源权限。
- API验收：匿名创建版本401，管理员缺CSRF发邀请403；正确邀请只显示一次，兑换为ENGINEER后可创建代码版本但发邀请403；退出后旧会话失效。公网Set-Cookie均含Secure。
- 公网模型Key即使ADMIN会话也返回403；页面在公网模式完全移除密钥输入，项目成员不能读取或替换值。
- 临时回环public-preview通过真实界面：匿名为公开只读；管理员登录并创建邀请；退出后用邀请码创建ENGINEER会话；开发写按钮启用，邀请面板隐藏，设置写入禁用。
- 邀请兑换弹窗在768宽无横向溢出。临时服务、账号、会话和邀请码随临时数据库关闭，不是公网部署证据。
- 新增3项认证测试并调整公开匿名边界回归，全量CI为149/149。详见[公网认证设计](public-auth.md)。

## 2026-09-15：云端MySQL/OSS持久化与冷启动恢复

- MySQL后端使用单项目`revision`条件更新保存平台文档和幂等记录；邀请、用户、会话、权限、版本、批次与审计均在同一控制面快照内恢复。
- 私有OSS业务状态对象使用首次`If-None-Match: *`、后续`If-Match: ETag`并发控制；快照覆盖离线落地行、实时事件/最新状态/Checkpoint、DAPI业务快照和报表数据快照。
- 云端协调器先持久化业务状态、再持久化元数据；每个变更响应等待协调器完成，后台实时事件和报表物化也通过写入监听触发保存。
- 冷启动API验收真实提交合成CSV源、连接测试、元数据扫描、同步任务与实际FULL落地；关闭全部内存数据库后重新打开，恢复1条运行证据和1条实际落地行。
- 陈旧MySQL/OSS写入均返回409，快照损坏、重复主键、悬空DAPI快照引用、非法JSON和超大对象失败关闭；签名请求不把Secret放入Authorization明文。
- 新增10项测试，全量CI 159/159，旧版和V2构建均通过。真实RDS/OSS/FC尚未连接，因此只记“代码与模拟云冷启动已验收”，不记公网已部署。
- 当前两存储提交只允许FC单实例试点；多实例生产部署需要事务性业务库或跨存储提交协议。设计与环境变量见[云端持久化报告](cloud-persistence.md)。

## 2026-09-15：公网隔离Spark执行协议

- 新增远程Spark客户端和独立Worker HTTP服务；公网控制面启动时先确认Worker为可用Apache Spark函数进程，未配置或健康失败继续显示不可用。
- 每次执行使用HMAC-SHA256绑定13位时间戳、24字节随机Nonce和完整JSON摘要；Worker限制时钟偏差并缓存Nonce，完全相同请求第二次返回409。
- Worker只接受`accounts`、`positions`、`cash`三张合成证券表，限制表/字段/行、请求/响应大小、单任务并发和运行超时；未知表即使签名正确也返回422。
- 客户端拒绝非HTTPS非回环地址、超大正文、无效JSON、非Spark结果，以及“状态成功但独立断言未通过”的响应；取消和超时分别返回明确错误。
- 远程Runner同时用于代码运行、交付文件演练和后续调度批次；Worker临时运行目录可配置为执行后删除，不把函数磁盘当持久证据。
- 新增6项协议测试及1项云交付版本测试，全量CI 166/166。当前只证明接口与本机安全语义，未创建FC Worker、未安装云端Java/PySpark、未验证私网/函数级鉴权或账单，因此不能记为公网隔离执行完成。

## 2026-09-15：Spark 3.5.9 Worker构建门

- 官方最新维护线核验后，没有把存在已修复漏洞记录的本机历史Spark3.5.7直接打入公网包；历史证据不改写，新云Worker锁定Spark3.5.9。
- `pyspark==3.5.9`与`py4j==0.10.9.9`均固定PyPI文件SHA-256，Linux构建启用`--require-hashes`；包内必须存在对应Spark SQL JAR。
- 云交付清单从实际远程运行读取3.5.9和`FUNCTION_PROCESS`，生成`remote-spark-worker-v1`；远程3.5.7即使断言通过也不能生成云包。
- 大包构建限制Linux x64/Python3.10、最大480MiB、拒绝秘密文件与覆盖已有ZIP；GitHub手动构建产物只保留1天，后续通过私有OSS交给FC，不走Base64 API上传。
- 本轮只完成构建配方和静态/单元门；GitHub工作流尚未实际运行，Java层ARN、FC私网/函数鉴权、真实Spark启动和费用均待云账号核验。详见[隔离Spark Worker](isolated-spark-worker.md)。
- 项目隔离的本机PySpark/py4j随后按固定哈希升级到3.5.9/0.10.9.9；35项真实Spark用例在新版本全部通过，23.891秒。该本机升级不改变上述GitHub/FC待办。

## 2026-09-15：不可变版本化交付产物

- 新交付包先把`manifest/files/digest`写入不可变产物存储，再写平台元数据；本机文件权限0600，云端对象路径由交付摘要推导，不接受提交路径。
- 私有OSS首次写使用`If-None-Match: *`；已存在对象重新读取，相同正文视为幂等，不同正文返回409且绝不覆盖。
- 产物引用固定driver、kind、digest、对象键、内容SHA-256和字节数；审批、文件演练和发布前都重新读取验证，不能只信平台记录或对象自报摘要。
- 历史本机包没有产物引用时，在首次审批/演练/发布动作中创建同内容不可变对象并补充引用，不修改源代码、清单或包摘要。
- 3项新增测试覆盖本机篡改、OSS幂等和摘要冲突；现有API测试确认创建包实际生成对象。全量CI 169/169。真实OSS权限、条件写、版本/生命周期仍待云端核验。

## 2026-09-15：公网管理员无明文引导

- `createPasswordHash`在本机使用与登录一致的scrypt参数和随机盐；云引导哈希严格校验算法、参数、盐16字节和派生值64字节。
- `shuzhan-password-hash`只从stdin读取密码，限制输入大小，不把密码写入参数、stdout或stderr；测试确认输出可用于真实登录且存储中不出现明文。
- 部署工作流秘密改为`V2_BOOTSTRAP_ADMIN_PASSWORD_HASH`；cloud-server若发现旧明文变量会在连接资源/监听端口前失败。
- 首次管理员哈希流程已验收；哈希只用于不存在该邮箱时创建账号，不会在冷启动时覆盖已有密码。
- 随后补齐登录后改密：必须验证当前密码和新密码强度，新密码不得相同；成功后撤销该用户全部旧会话并签发新会话，记录不含秘密的审计。设置页提供不保存密码的表单。全量CI 170/170；忘记密码的管理员恢复流程仍待实现。
- 临时回环public-preview实际登录管理员：错误的两次新密码立即显示表单错误；正确改密显示成功和新会话，退出后旧密码被拒绝、新密码登录成功。随后关闭临时标签和服务；账号与密码均为本次虚构临时数据。

## 2026-09-15：应用内月度预算门

- 汇总本月九类Agent资源；有prompt/completion usage时按qwen3-coder-plus输入档位分别计价，只有total或缺usage时使用保守上界，未知模型使用最高配置单价。
- 模型任务在持久记录创建前检查一次，真实模型每次调用前再次检查，避免排队期间其他任务已耗尽额度；超限返回`MONTHLY_MODEL_BUDGET_EXCEEDED`。
- 远程Spark Runner在调用前同时检查本月实际运行数、累计秒数和下一次30秒预留；超限返回`MONTHLY_SPARK_BUDGET_EXCEEDED`。
- 可选的已核验账号累计支出达到200元时，模型与远程Spark都返回`MONTHLY_HARD_BUDGET_EXCEEDED`；未接入时状态明确`NOT_CONNECTED`并提示不能证明全站费用。
- API测试在预算关闭时确认HTTP429、模型调用0、Agent记录0；CLI `shuzhan budget`和MCP `budget_status`读取同一`/api/v2/budget`。设置页以第四张卡片展示模型估算、Spark次数、硬上限和账单连接状态。
- 新增4项测试，全量CI 174/174。当前账号支出仍是部署前核验输入，不是自动账单API，故总费用硬停尚未完成真实云验收。
- 重启本机服务后实际打开设置页，预算卡读取现有持久记录显示模型估算¥0.146/¥50、远程Spark 0/200、硬上限¥200，并明确“账号账单尚未接入，不能证明全站费用”；不是静态预设的“0元”。

## 2026-09-15：阿里云只读预检脱敏器

- 新脚本只调用STS身份确认、RDS属性/网络/库/账号Describe、FC GetFunction、OSS GetBucketInfo和BSS账单总览，不含创建、更新、启停或删除命令。
- 输出只包含规格/状态、目标库和账号的布尔存在性、环境变量键、账单金额及同VPC/单并发/预算交叉检查；原始响应在随机临时目录，退出自动删除。
- 假CLI响应内放入账号、ARN、连接地址、Bucket、角色、VPC、函数/RDS名称及环境秘密值；测试确认最终JSON均不含这些诱饵，且报告权限0600。
- 新增1项测试；随后使用本机官方CLI 3.5.0和浏览器OAuth完成真实只读执行。RDS为STOPPED、Serverless MySQL8、0.5—2 RCU、20GiB、自动暂停、1个内网/0个公网端点；暂停态库账号查询返回`InvalidDBInstanceStatus.NotSupport`，均保留null。
- 2026-09账单税前0.59元、支付0元、未付0.59元，当前低于200元。官方ossutil 2.3.0安装包SHA-256核验通过；账号唯一Bucket候选为杭州/Standard/private/版本未配置，但FC仍返回AccessDenied，故尚不能证明函数绑定、同VPC、单并发和平台库就绪。全量CI当前181/181；详见[只读预检](aliyun-readonly-audit.md)。

## 2026-09-15：M4h数据契约闭环

- 从`landing:raw_positions`实际字段和资产证据创建`raw_positions_contract` V1；实际读取5行，结构差异0、行通过率100%/目标99%、新鲜度低于86400秒，结果PASSED。
- 可空`currency`新增被判兼容并创建V2；因实际资产缺少该字段，复检FAILED并产生不含业务值的告警。
- 删除`currency`被判破坏性，页面在未确认前禁用创建；确认后生成V3，复检PASSED并以恢复检查ID解除原告警。V1/V2、失败检查和确认记录均保留。
- 未配置实际行或无法度量新鲜度时返回PARTIAL；只有完整PASSED可解除告警。BACKWARD新增必填字段同样判为破坏性。
- GUI实际完成V1→V2失败→V3恢复；API、CLI、MCP共享6个契约操作。Node全量测试181/181，TypeScript/Vite构建通过。当前仍是本机合成范围，不是公网或生产契约中心。详见[M4h报告](m4h-data-contracts.md)。

## 2026-09-15：真实RDS平台元数据库准备

- 经用户批准短暂唤醒现有Serverless RDS；运行态确认`business_demo`和`sync_writer`仍存在，随后创建独立`platform_meta`并复核其状态Running，未新购实例。
- 新增安全账号辅助脚本：两次隐藏输入且先本机校验；必要时自动唤醒AutoPause实例；固定创建Normal账号`platform_app`并只授`platform_meta` ReadWrite；已存在时拒绝覆盖，密码不写文件、不回显、不进入Shell历史。
- 用户通过macOS隐藏弹窗完成密码输入；脚本必要时唤醒RDS，真实创建账号并授权。最终Describe显示`platform_app`为Normal/Available且`platform_meta` ReadWrite，`business_demo`/`sync_writer`仍在，`platformDatabaseReady=true`。
- 1项假CLI测试验证操作目标和输出脱敏，全量CI 182/182。当前只完成RDS控制面，不代表本机或云函数已成功建立数据面连接。

## 2026-09-15：M2交付发布多端契约补齐

- 复核发现原V2 CLI/MCP只有发布批次读取，不能支撑工程师或Agent完成“交付包→演练→审批→发布→监控→回滚”，因此撤销此前笼统的多端完成假设。
- 新增14项共享操作：交付包列表/详情/创建/演练，演练列表/详情/取消，审批列表/提交，发布列表/详情/创建/回滚及发布监控；CLI参数映射和MCP JSON Schema均调用现有`/api/v2`。
- MCP对取消、审批、发布和回滚明确标注需用户确认；本机发布依然返回非公网边界。接口单测实际对比CLI/MCP读取同一空交付包列表，并验证创建和审批请求体。
- 新CLI实际读取现有持久状态：3个M2A交付包，最新包摘要为64位且未发布；发布监控为成功8、失败2、开放告警0、当前HEALTHY，同时明确`LOCAL_RELEASE_MONITORING`、`publicDeployed=false`、`fullLifecycleE2E=false`。

## 2026-09-15：公网部署证据门与FC只读诊断

- V2专用手动工作流已移除硬编码云账号/VPC/vSwitch/安全组标识，所有目标来自受保护Environment；函数配置改为只写已知白名单环境，不继承旧环境里的秘密。
- 部署前读取`docs/evidence/`单个脱敏报告，要求当月且24小时内、账单税前小于200元、RDS运行/独立平台库账号、杭州私有OSS、专用函数同VPC/角色/并发1、ICP备案/域名归属/HTTPS，并用函数名/域名哈希对应目标。OIDC授权后还实时再读BSS账单，拒绝旧金额假设。
- 当前真实报告运行预检明确拒绝：专用函数未验收、同VPC未验收、域名ICP备案未验收；本机或账单通过不能转写为公网已部署。
- 官方FC 3.0 SDK只读诊断：本机OAuth配置0600且短期STS在SDK内部存在；实际GetFunction返回AccessDenied/缺SecurityToken类别，函数规格仍为UNAVAILABLE。测试仅输出规格、布尔或错误码，不输出账号、函数名、Bucket、地址、Key/Token。

## 2026-09-15：代码Agent七阶段证据旅程

- 现有Agent委托只覆盖SQL开发；新增只读`/agent/tasks/:id/journey`从同任务attempt、代码版本和Spark运行关联交付包、调度/部署文件、演练、摘要审批、墙上时钟批次、健康与告警，七阶段标明Agent/工程师/调度器责任。
- 合成实测将成功模型+Spark任务依次接上不可变交付包、成功文件演练、审批、发布与两个计时批次；本机证据完整可显示，但`agentIndependentE2E=false`、`evaluatedAsFullLifecycle=false`、`publicDeployed=false`不改变。
- 开放告警使当前监控阶段降为待完成；回滚的历史发布降为HISTORICAL。响应不含SQL、业务行或原始报错；CLI/MCP共用同一只读API，开发Agent面板可展开阶段和短证据ID。
- 真实本机Qwen+Spark历史委托在独立3101端口展示需求/代码/调试3阶段完成、调度/部署/发布/监控4阶段待完成；责任归属为中文，摘要紧凑排列，第三阶段展开只显示运行ID与边界说明。`document.scrollWidth=innerWidth=1280`。
- 首次3101页面白屏定位为本机Origin端口固定3100/5173导致同源请求403；改为当前监听回环端口后真实页面正常加载，外部来源仍403。独立3101只用于不干扰现有3100的验收，不是公网性能或多网络证据。
- 全量189/189通过后，准确停止旧3100进程并恢复原端口；主工作台实际点击“查看/刷新七阶段证据”成功、`document.scrollWidth=innerWidth=1280`。V2 CLI和MCP均从同一历史任务读到7阶段、`agentIndependentE2E=false`、`publicDeployed=false`；临时3101服务随后关闭。

## 2026-09-15：Agent受控交付准备与所选包监控隔离

- 真实Qwen委托`4a412b2e…`生成虚构证券财富顾问T+1 SQL：一次调用，1317 Token；Spark3.5.9运行`4b8d0597…`约9.395秒，五套独立回归5/5。
- 自动准备`c7a89142…`复用该已验证代码生成8文件不可变包`e5d7f494…`，包摘要`c61d82de…`、schedule/deployment/tests各自哈希可核对；按文件演练`9cd8b4e6…`再次用Spark3.5.9运行约9.345秒，main/tests与5回归真实通过，三DAG节点全SUCCEEDED且非测试替身。
- 新包审批0、发布0；任务停在AWAITING_ENGINEER_REVIEW、`completionScope=DELIVERY_PREPARATION`、`agentIndependentE2E=false`、`fullLifecycleE2E=false`、`publicDeployed=false`。七阶段前五步完成，后两步仍待工程师/调度器。
- GUI实际从Agent面板点击“审阅交付包再决定审批”进入调度工作台，准确选中`e5d7f494…`、显示文件与真实演练、明确“未审批发布”。未发布新包不再继承其他历史健康版本的M2c步骤或回滚按钮；所选历史包则显示历史而非当前监控。
- 3100主服务加载新版后，真实Agent旅程显示前五阶段完成，调度文件责任为“Agent受控编排 · 工程师审阅”，文件演练为“Agent编排 · Spark真实演练”，审批/监控待完成；1280宽度无页面级横向溢出。开发面板同时显示“交付文件已准备”和审阅入口，不再把已有准备误写为“当前仅代码阶段”。
- V2 CLI `agent delivery --id`与MCP `agent_delivery_detail`读取同一准备任务：SUCCEEDED、AWAITING_ENGINEER_REVIEW、`actualExecution=true`、`agentIndependentE2E=false`、`publicDeployed=false`。
- 浏览器实际切换旧M2c告警包`37f8c246…`：显示“历史发布版本、非当前生效”，CRITICAL及旧失败批次均绑定本包，步骤4不标完成且不提供当前版本回滚操作；切回新Agent包`e5d7f494…`，步骤4仍未完成，监控提示本包未审批发布。
- 新回归对同一源Spark运行先置入旧已审批包，再启动Agent交付准备；后台生成不同新包，旧审批仅绑定原包，新包审批数0，旧批准不能直接转移。

## 2026-09-15：M2b独立工程师审阅关口

- 原审批只携带备注，不能独立证明工程师已经核对了哪些内容；现拆分为`delivery_review`。记录严格绑定交付包摘要与成功按文件演练编号，逐项确认代码、独立断言、DAG/部署文件和“仅本机合成数据”范围；不创建发布资格。
- 审批请求必须引用同一包、摘要、演练均一致的`reviewId`。缺少审阅记录返回400；遗漏任一确认返回422；不同包或不同演练的审阅返回409。已有包摘要不变时可幂等读取同一审阅，变更代码仍必须重新生成包、演练和审阅。
- GUI实测最新Agent准备包`e5d7f494…`：显示四项可访问复选框、审阅备注、独立“记录审阅”按钮，以及其后的审批与发布按钮。未作任何勾选、审阅、审批或发布；1280宽度`document.scrollWidth=innerWidth`。
- API、CLI、MCP同源：CLI须显式传入四项`--attest-*`，MCP JSON Schema要求四项`true`并警示实际审阅和明确确认。该本机关口不改变`agentIndependentE2E=false`、`fullLifecycleE2E=false`或`publicDeployed=false`。
- API/CLI/MCP同源；匿名公网创建401、假模型/假Spark失败关闭、取消后不能变成功、服务重启进行中准备转INTERRUPTED。全量CI最新194/194。详见[Agent交付报告](agent-delivery-preparation.md)。

## 2026-09-16：独立 V2 FC 创建前置与 Linux 包门禁

- 用户明确授权“创建独立 V2 FC 函数，严格按预算和最小权限部署”。先在 Cloud Shell 只读复核：7 个 RAM 角色、2 个 FC 信任角色、1 个 V2/部署命名候选；RDS 所在 VPC 有可用交换机和安全组；FC 函数仍为 3 个且无 V2 专用目标；RDS 保持 STOPPED。没有复用旧函数，也没有再次唤醒 RDS。
- 分支凭证扫描未发现已跟踪的 API Key/AccessKey/密码值，已推送到独立分支。为便于 Linux 门禁，CI 增加手动触发入口；GitHub Linux x64 CI `35063000879`完成 Node24、`node:sqlite`、入口、网页和 fixtures 的构包校验。
- 完整函数 ZIP 未额外上传为长期 Artifact；Cloud Shell 的系统为 glibc 2.27，官方 Node24 二进制要求 glibc 2.28，不能把 Node14/Node22 降级包当作 V2 交付。因此当前仍未创建占位函数或更新任何现有函数。
- 创建门仍需同一受控 Linux Runner 内取得包并同时具备专用角色/同 VPC 绑定、云端 MySQL/OSS/管理员受保护配置及已备案 HTTPS 域名；这些条件未齐全前，`publicDeployed=false`和云就绪门失败是正确状态。
- 已新增手动 `provision-v2-staging.yml`：创建前检查账单、FC 执行角色信任与宽权限文本、同 VPC vSwitch/安全组和目标函数不存在；只构造 `CreateFunction`，不覆盖已有函数。工作流尚未触发，未产生云写入。
- 设置页在受保护部署证据返回后提供 `v2-staging` 配置入口；该链接只在认证读取到部署状态时渲染，匿名公网请求仍先经过后端认证门。
- 新增 `verify-v2-staging-config.mjs` 与 2 项测试：缺少秘密、非 HTTPS、Origin 不一致或部署/执行角色相同均失败关闭；返回只含键名/固定错误码，不含任何配置值。空环境实跑返回18个缺失键并以非零状态退出；工作流静态门和 V2 构建均通过。
- 本轮完整 `npm run ci` 在受控本机环境重新通过209/209；Cloud Shell、FC、RDS、OSS和公网状态未被该本机质量门覆盖。
- 新增无值配置清单 [v2-staging-environment-template.md](v2-staging-environment-template.md)，区分 GitHub Variables 与 Secrets，并明确隔离 Worker 首期留空。
- 已创建 GitHub `v2-staging` 环境并实际写入9项已核验非秘密变量；环境 Secret `JASONSECRETS` 已存在（只读取名称，不读取内容）。
- `JASONSECRETS` 兼容已接入预置/部署工作流：支持白名单 JSON 或单行 KEY=VALUE，逐项 Secret 优先；本轮完整 CI 213/213 通过。
- PR [#60](https://github.com/Jason-ZhangHaobo/data-platform-demo/pull/60) 已合并；配置门修复在增量 PR [#62](https://github.com/Jason-ZhangHaobo/data-platform-demo/pull/62) 中，尚未合并。
- 已打开阿里云万网域名注册与 ICP 备案入口；因当前没有已备案 HTTPS 域名，域名购买、实名模板、主体备案和支付仍由用户完成。协作边界见 [domain-registration-checklist.md](domain-registration-checklist.md)。
- 本轮独立重跑 Spark/进程门：Spark 3.5.9 的 35/35 通过，取消/超时生命周期 3/3 通过；范围仍是本机引擎与进程，不升级为云隔离或公网 E2E。
- 本机设置页浏览器验收：侧栏完整显示 Data Agent、数据源、数据同步、数据开发、调度与发布、数据资产、数据质量、安全与脱敏、数据服务、数据报表、运维监控和平台设置；部署门显示账单摘要与6项待补齐，并显示 `配置 v2-staging` 入口。页面未渲染任何账号、地址、资源ID或秘密。
- 分支推送后远程 GitHub Linux CI `35075342577` 已成功完成，验证 Node 24 FC 构包门、209 项测试、旧版构建和 V2 构建；该 CI 仍不执行阿里云写操作。
- 最新快速失败工作流改动再次通过远程 GitHub Linux CI `35077273405`（34 秒）；配置缺失仍会在依赖安装/构包前失败。
- 已创建不自动合并的 [Pull Request #59](https://github.com/Jason-ZhangHaobo/data-platform-demo/pull/59)，用于审阅并让新增 V2 FC 预置工作流进入默认分支；PR 本身不触发阿里云写入。

## 2026-09-18：Secrets 到位与预置门修复

- 只读核验确认 `v2-staging` 中存在名为 `JASONSECRETS` 的环境 Secret；不会读取、回显或写入其内容。
- 首次预置运行 `35290798356` 在“配置校验”阶段失败关闭，未安装依赖、未构包、未连接阿里云、未创建函数；失败根因是 CLI 入口未把 `process.env` 传入校验函数。
- 修复已放入增量 PR #62；本机配置测试8/8、完整 CI 213/213。PR 合并后再重跑预置，不把失败运行算作云端资源状态。

## 2026-09-19：持久调度核心

- 新增外部tick驱动的`DurableReleaseScheduler`，没有调用`setTimeout`。针对性测试确认执行器被调用前元数据已保存RUNNING租约，成功后清除租约并保存监控结果。
- 受控过期RUNNING租约在下一次tick恢复并只执行一次；同库中的ACTIVE_LOCAL计划仍保持SCHEDULED，不会被云端tick消费。
- 签名入口绑定独立协议域、时间戳、Nonce和原始正文；错误签名401，同一Nonce重放409。Nonce短窗口写入元数据，模拟冷启动后仍可恢复。
- 外部GitHub cron工作流由`V2_SCHEDULER_ENABLED`显式开启，默认跳过；当前没有域名、调度密钥、ACTIVE_CLOUD发布或真实云Spark，因此本轮只计核心和API本机验收，不计云调度或公网E2E。
- 当前完整`npm run ci`为221/221，并完成旧版构建和V2 TypeScript/Vite构建；该数字只证明本机代码与故障模型，没有触发定时工作流或云资源。

## 2026-09-19：隔离Spark Worker W1 Linux包

- 手动工作流`35427706320`在GitHub Linux Runner于53秒内完成协议测试、Python3.10哈希依赖安装、Spark3.5.9 JAR检查、ZIP完整性/秘密文件/480MiB上限检查和短期Artifact上传。
- 产物大小318,451,671字节，GitHub记录SHA-256为`a2becee6…c3133`，到期时间为2026-09-20；未下载到聊天、未提交Git、未上传OSS或部署FC。
- 本次只将W1标为通过；Java17层、Worker进程健康、私网/函数鉴权、单任务、故障恢复与公网E2E仍分别属于W2—W5。
- 运行暴露checkout/setup-node/setup-python/upload-artifact的Node20弃用提示；工作流已切换到官方Node24 major版本，必须再运行一次确认告警消失后才合并。
- Node24 Action复验`35428122388`成功且只剩Ubuntu镜像迁移notice；包大小仍为318,451,671字节，但摘要改变为`e4afa3b2…c38e`。因此发现并记录了不可复现ZIP缺陷，没有把“依赖有哈希”误写为“整个包可复现”。
- 构建脚本现固定所有文件时间戳、按字节序排序并用`zip -X`去除扩展属性；双构建摘要一致性仍待远程复验。
