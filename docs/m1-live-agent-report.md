# M1 真实模型与 Spark 试跑报告

日期：2026-09-14。模型 qwen3-coder-plus；执行引擎 Apache Spark 3.5.7；输入为本项目虚构证券账户、持仓、现金数据。
范围仅为 **代码生成 → 执行反馈 → 自动修正 → 业务结果验证**，不包含真实调度、部署、上线和运维监控。

## 试跑过程与实际缺陷

| 试跑 | 模型请求次数 | 结果 | 处理 |
|---|---|---|---|
| A：69253b75-cc15-4f51-945f-0d1999ac1585 | 3 | 失败；17.857秒 | 本地 sqlglot 将 AND/OR 归入 Func，校验器误拒合法逻辑条件；已修复并测试嵌套危险函数仍被拦截 |
| B：0601d4c0-0a06-41fa-9bcc-d3848323a20a | 3 | 当时三套样例通过；37.566秒 | 发生 ROW_NUMBER 不支持和字段缺失错误，模型逐次修正；代码复核发现同额不同持仓、纯现金客户两个盲点，不算当前五场景门槛通过 |
| C：9cc8bf9c-a1a2-426f-9065-538ae86fe951 | 2 | 五套样例全部通过；35.599秒 | 首次引用 positions.advisor_id 报错，Agent 读取真实引擎报错后改为通过 accounts 关联，第二次成功 |

没有删除失败记录，也没有把三次试跑转为一个有代表性的成功率。它们的代码/校验范围不同，并非已冻结的正式评测集。
接口故障、输入能力范围和业务边界由本轮证据驱动修正；不得把语法保护器问题归结为密钥错误或推定模型整体不行。

## 当前通过的版本

- Agent 任务：9cc8bf9c-a1a2-426f-9065-538ae86fe951。
- Spark 运行：3ad518cd-9b3f-46d1-a284-b45dff1062ff，8.419秒（包含五场景核验）。
- SQL 版本：f7c3a0be-a2b8-490d-ab1a-fa1ba2d6db1b。
- SQL SHA-256：b7a73bf028c70deab841bab97423f371ab23d143ad893dcd4f2d72d2d15764fa。
- 验证契约：securities-assets-five-fixtures-2026-09-14。
- 导出验证包：requiresRevalidation=false，releaseState=NOT_PUBLISHED。

| 独立样例 | 核验点 | 结果 |
|---|---|---|
| 标准数据 | CLIENT-001 市值1500、现金300、总资产1800、证券数2 | 通过 |
| 现金变更 | 同一 SQL 计算总资产2300，不固定返回1800 | 通过 |
| 重复持仓 | 同一个 position_id 的重复数据不重复计入 | 通过 |
| 同额不同持仓 | 不同 position_id 即使证券及金额相同仍计入，总资产2800 | 通过 |
| 仅有现金客户 | CLIENT-004 现金100、持仓0、总资产100、证券数0，客户ID非空 | 通过 |

预期结果由独立测试契约提供，不传给模型。任务只被标为 SQL_DEVELOPMENT，fullLifecycleE2E=false。
这五套样例不能覆盖所有业务变化，更不能代替最终至少20个完整任务场景、≥85%的 E2E 评测。

## 用量与费用

三次诊断试跑合计8次真实请求：输入8202 Token、输出2982 Token，共11184 Token。
按北京地域、输入不超过32K档的 [官方价格](https://help.aliyun.com/zh/model-studio/model-pricing)（输入4元/百万、输出16元/百万）估算：
8202×4/1,000,000 + 2982×16/1,000,000 = **0.08052元**。
未计缓存折扣、免费额度或优惠，不是账户实际出账金额；也不包含既有 FC/OSS/RDS 的成本。

## 查看与复现

进入 [本机代码工作台](http://127.0.0.1:3100/v2/?module=development)，查看最近的 Agent 委托、审阅生成代码，然后切换下方“验证”和“运行记录”。
SQL、首次报错、修正产物、引擎结果和契约版本均保存在当前项目的本地平台库；可导出验证包。
单元/回归测试通过55项，独立 Spark 检查通过29项；测试替身与真实模型请求分别记录。

## 冻结20例扩展评测

评测契约：`m1-securities-code-stage-20-cases-2026-09-14`；场景集摘要`c97cce1…a7e`。4类开发意图（最小草稿生成、现金重复修复、纯现金客户修复、错误字段/范围修复）与5套合成数据上下文交叉，共20例。每例最多三次模型修正，最终SQL必须在真实Spark上通过全部5套回归。

首轮`34c20547-6760-4def-9840-e8f442028106`显示4/20，但只有6次模型尝试；16例在提交前因评测幂等键超过API 100字符限制而失败。该结果保留为评测器缺陷证据，不解释为模型失败。修复为固定长度SHA-256幂等键后，使用完全相同的冻结场景集复跑。

正式复跑`a6ccf750-836d-4f1b-8e73-a66da710e1bb`：20/20完成，代码阶段完成率100%，共33次模型尝试、报告37,559 Token；失败0、未执行0。结果保存在`.v2-artifacts/evaluations/a6ccf750-836d-4f1b-8e73-a66da710e1bb/report.json`，生成物不提交Git。

该结果的适用边界很窄：20例是“4类同一客户资产输出契约×5套数据”，不是20种数仓业务，也没有逐例完成调度、上线和监控；`fullLifecycleE2E=false`。后续完整E2E评测需扩展不同指标与输出契约并贯通M2/M3。

M1当前冻结范围已经完成；M2a及M2b/M2c本机标杆链路后续证据分别见[m2a交付](m2a-delivery.md)与[m2b/m2c本机发布](m2b-m2c-local-release.md)。当前未部署公网、未生成最终PPT。
