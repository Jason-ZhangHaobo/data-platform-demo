# M3：DAPI/XAPI 数据服务

日期：2026-09-14。当前范围为本机、虚构证券数据和单项目开发身份；不是公网或生产数据服务。

## 已实现链路

1. 从状态为SUCCEEDED、由墙上时钟调度、发布且独立断言通过的Spark批次选择结果。
2. 将客户资产结果写入独立业务SQLite快照；同一快照ID不能对应不同内容，十进制金额保持字符串两位精度。
3. DAPI声明返回字段、client_id可选参数、分页、超时和每分钟限流；实际查询后保存测试结果摘要。
4. 只有当前配置哈希存在成功测试才能发布；新版本与历史版本不可变，支持版本切换。
5. XAPI固定2—5个已发布DAPI的精确版本，首版按client_id声明式合并，不执行任意代码。
6. OpenAPI 3.1描述Bearer、参数、200/401/403/429/504响应。
7. 应用授权令牌仅显示一次，平台只存哈希；相同幂等请求不重复发令牌，丢失后撤销并重建。
8. 外部调用保存服务版本、应用、参数、状态码、耗时、行数和结果摘要，不记录令牌。
9. GUI、共享V2客户端、`shuzhan` CLI与`shuzhan-mcp`使用同一`/api/v2`资源；旧V1接口继续明确为模拟。
10. Data Agent读取已发布资源摘要，生成DAPI/XAPI方案；后端验证资源ID、版本、字段、超时和限流，人工应用后只创建草稿。

## 实际本机证据

- 源发布批次：`7b1e764d-b655-4c14-a315-becf000a6123`，Spark3.5.7、调度触发、结果验证通过。
- 持仓DAPI：`fcff08ca-fef1-446a-ab76-ccdb3afef90f`，V1 `031f1214-d479-401e-9e7c-142020216de1`；CLIENT-001返回持仓市值1500.00、证券数2。
- 现金DAPI：`a964e250-b316-4b52-ab85-aa4c1e4c20f1`，V1 `c3ba30c0-ec91-4439-8863-7ea6e03421a0`；CLIENT-001返回现金300.00、总资产1800.00。
- 已发布XAPI：`a4427ed5-269e-40e1-abb5-322edede5169`，V1 `aa6baa72-6e40-4475-adeb-d6af2a615a51`，固定上述两个DAPI版本。
- 示例外部系统调用持仓DAPI得到CLIENT-001实际结果；调用XAPI得到CLIENT-002持仓750.00、证券数1、现金200.00、总资产950.00。
- 调用记录：DAPI `32348c2b-b0f2-4f0a-9de2-9c41ffa6729d`、XAPI `07736af6-6638-4b7f-a83c-a63550dec939`，均为V1、HTTP200、1行、约1ms。
- 验收应用`5f1d36b1-7d83-48ec-ac53-b6bad530dd4f`的明文令牌临时文件已删除，应用随后标记REVOKED，未留下无法管理的有效权限。
- 真实Data Agent方案`a20fe4a8-3e24-41f1-9df7-24e8d8306556`正确引用两条DAPI精确版本，模型qwen3-coder-plus，输入1185、输出223、合计1408 Token。
- 人工应用为XAPI草稿`7a941077-87fe-4e1d-b463-3cf8a93b3367`，路径`advisor-portfolio-insight`；遵照方案未测试、未发布、未发令牌。
- V2 CLI实际读取两条DAPI及已发布XAPI OpenAPI；V2 MCP通过`xapi_list`读取一条已发布和一条草稿XAPI，均未访问V1模拟路径。

## 自动测试与尚待收尾

针对性测试覆盖独立业务快照、精度、参数分页、DAPI/XAPI版本、OpenAPI、一次性令牌、最小授权、撤销、持久化限流、超时、日志、模拟源拒绝、Agent方案约束以及CLI/MCP同源API。

当前仍需完成浏览器响应式验收、实际版本切换与限流调用证据以及全量CI，完成后才能将“本机M3核心”改为“本机M3已验收”。云端数据库、受邀身份和公网端点仍属于后续门槛。
