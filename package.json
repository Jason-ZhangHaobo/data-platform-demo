{
  "name": "data-platform-demo",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "dataplatform": "bin/dataplatform.mjs"
  },
  "type": "module",
  "description": "A zero-dependency data platform demo for practicing the full delivery loop.",
  "dependencies": {
    "mysql2": "^3.11.5"
  },
  "scripts": {
    "dev": "PORT=3000 node --watch src/server/index.mjs",
    "build": "node scripts/build.mjs",
    "start": "PORT=3000 node src/server/index.mjs",
    "lint": "node scripts/check-source.mjs",
    "test": "node --test",
    "ci": "npm run lint && npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
